# 2026-08-28 這一輪的完整發現

> **這份文件的定位**:交易線第一個完整回合的**實驗紀錄**。
> 入口仍然是 [`HANDOVER.md`](HANDOVER.md)(那裡只放結論與下一步);
> 資料欄位與坑在 [`DATA.md`](DATA.md);**這裡放「做了什麼、怎麼做的、為什麼相信」**。
>
> 全部數字都可重跑:`new_src/models/baseline.py`(A)、`new_src/trading/*.py`(B)。

---

## 0. 一頁摘要

| | 模型 A:預測日前電價 | 模型 B:預測不平衡價差 |
| --- | --- | --- |
| 目標 | 明天每小時的日前出清價 | `dev = 不平衡價 − 日前價` |
| 結果 | ✅ **rMAE 0.66/0.72 → 0.58/0.58** | 🔴 **回收上界 ≈ 0,三輪都是** |
| 最終 R² | **0.754 / 0.736** | **−0.003 / +0.001** |
| 病因 | — | **訊號不存在**(不是漂移、不是模型、不是特徵不足) |

🔑 **最重要的一句**:同一批特徵、同一個模型、同一段期間 —— 預測**電價** R² 0.75,預測**價差** R² 0.00。

---

## 1. 模型 A —— 預測日前電價

### 1.1 原本是什麼

`new_src/models/forecast.py` + `baseline.py`,四個模型並排:

| 模型 | 算式 | 角色 |
| --- | --- | --- |
| naive-24h | `ŷ = 昨天同一小時的價` | 零學習,**當尺**(rMAE 的分母) |
| Ridge | 線性 + 懲罰權重平方 | |
| Lasso(LEAR) | 線性 + 懲罰絕對值 → 自動挑特徵 | **電價預測文獻的標準強基準** |
| LightGBM | 梯度提升樹 | |

**設定**:訓練**一次**(切在 2024-07-01)、**一個模型吃 24 個小時**、目標是價格水準。

**超參數**:Ridge `alphas=logspace(-2,4,25)`;Lasso `LassoCV`;兩者用 `TimeSeriesSplit(5)` 選;
LightGBM `n_estimators=3000, lr=0.03, num_leaves=63, subsample=0.8, colsample=0.8`,
early stopping 50(驗證集取訓練期**尾段** = 訓練的未來,不 leak)。

**特徵 50 個**:行事曆 · 天氣(風速 100m、陣風、風向、三種輻射、雲量、氣溫)·
風/光日前預測 · 負載預測 · 鄰國(德/瑞)風光與殘餘 · 聯絡線容量 ·
燃料(TTF 天然氣、EUA 碳價、API2 煤、歐元匯率)· 價格 lag24 / lag168。

### 1.2 一開始撞到什麼

**① 文件裡的數字失效了。** `BATTERY_TRACK.md` 記「LightGBM 最好,MAE 17.20、rMAE 0.54」。
資料延到 2026-08-21 之後同樣設定重跑,**LightGBM 掉到 25.54 / rMAE 0.84,被 Lasso 反超**。

**② 🔴 找到一個真 bug:一格 NaN 把整把尺毒掉。**
2025-10 夏令時那一小時的 `price_lag24_eur` 是 NULL,而 `np.mean` 不是 `nanmean`
→ naive-24h 的 MAE/RMSE/R²/rMAE **全部變成 `nan`**,**rMAE 這把尺等於壞掉不知道多久**。
修法:`load_training` 加上 `price_lag24_eur IS NOT NULL`,順便保證四個模型評在**同一批列**。

### 1.3 掃了什麼

`new_src/models/experiments.py`,**44 個設定**,三個維度:

| 維度 | 選項 |
| --- | --- |
| 重訓 | `once`(訓練一次)· `monthly`(逐月、擴張窗)· `roll12`(逐月、**只用最近 12 個月**) |
| 建模 | `pooled`(一個模型吃 24 小時)· `hourly`(**逐小時 24 個模型**,LEAR 的標準做法) |
| 目標 | `level`(價格水準)· `detrend`(**相對昨天同一小時的變化**,預測完再加回去) |
| 模型 | Ridge · Lasso · LightGBM · 三者平均 · 僅線性平均 |

🔴 **判定標準不是「MAE 比較低」**,而是與基準做**配對的按日區塊 bootstrap**:
重抽「天」,每次算兩個模型在**同一批天**上的 MAE 差,95% 區間跨 0 就不算改進。
配對很重要 —— 兩個模型共同的好日子壞日子會抵銷,剩下的才是真實差距;
按「天」重抽是因為電價日內高度相關,逐小時重抽會低估標準誤好幾倍。

### 1.4 🔑 中途的自我更正:我的假設只對一半

第一版網格挑出「逐月重訓 + 逐小時」為冠軍,我當時的解釋是「LightGBM 只是模型過期」。
**但那個冠軍是在同一段測試期上挑的** —— 跟我要求 B 的標準不一致。加上前後半期拆解之後:

> 🔴 **LightGBM 在前半期(2024-07→2025-07)其實是全場最強**(DK1 MAE 16.67),
> 後半期崩到 26.03。**不是「樹比較差」,是有一個斷點。**

### 1.5 🔑 新發現:2025-10 的日前 15 分鐘改制,在電價預測上留下結構斷點

逐月追偏誤,斷點正好在 **2025-10**(日前市場轉 15 分鐘,交割 10-01)。
之後 DK1 電價月均從 **79 升到 97**,三個模型分道揚鑣:

| 2025-10 之後的平均偏誤(預測 − 實際) | |
| --- | --- |
| Lasso 逐月重訓 | **−2.0** |
| Lasso 訓練一次 | −6.3 |
| **LightGBM 逐月重訓** | **−23.3** ← 連重訓都救不了 |

**機制:樹的葉子值是歷史目標的平均,不能外推價格水準**;而擴張式訓練窗把新制度
稀釋在七年舊資料裡。線性模型靠燃料價係數就把整條預測抬高了。

✅ **兩個對症修法都有效而且疊加**(DK1、pooled):

| LightGBM 設定 | MAE | 2025-10 後偏誤 |
| --- | --- | --- |
| 逐月重訓(原本) | 21.33 | −23.30 |
| 只用最近 12 個月 | 18.33 | −12.96 |
| 改預測「相對昨天的變化」 | 19.60 | −17.40 |
| **兩個都做** | **17.70** | **−9.60** |

### 1.6 🔴 第二個自我更正:兩個 leak(而且是 repo 原本就有的)

使用者質問投標時點時查出來的:

| 特徵 | 是什麼 | 合法嗎 |
| --- | --- | --- |
| `price_lag24_eur` / `price_lag168_eur` | 昨天/上週同一小時的**日前價** | ✅ D−2 12:55 就公布 |
| **`load_lag24_mwh`** | 昨天同一小時的**實測負載** | 🔴 目標在中午後 → 關門(D−1 12:00)之後才量到 |
| **`residual_lag24_mwh`** | 同上 | 🔴 |

拿掉之後顯著變差(ΔMAE CI DK1 [+0.47, +0.79]、DK2 [+0.49, +0.85]) → **它們確實在偷分**。
已補進 `forecast.LEAK_COLS`,並加上**合法替代** `*_lag48`(D−2 同一小時,關門時一定已公布)。

⚠️ 這兩欄是 **duckdb `training` view 原本就有的**,所以舊基準也一樣虛高 → **改進幅度不受影響**。

### 1.7 ✅ 最終結果(去 leak,`baseline.py` 可重現)

**★ BEST = Ridge + Lasso + LightGBM 三者平均 · 只用最近 12 個月重訓 · pooled · detrend**
(兩個價區挑出同一個設定)

| | 基準 Lasso(訓練一次) | ★ BEST | 改進 |
| --- | --- | --- | --- |
| **DK1** | MAE 20.09 · rMAE 0.66 · R² 0.706 | **17.60 · 0.58 · 0.754** | **−12.4%** |
| **DK2** | MAE 23.10 · rMAE 0.72 · R² 0.661 | **18.70 · 0.58 · 0.736** | **−19.0%** |

配對按日 bootstrap 的 ΔMAE:**DK1 [−3.09, −2.10]、DK2 [−3.94, −3.05]**,都遠離 0。

🔑 **它勝出的理由不只是平均低,是它在斷點後不退化**:前半 17.14 / 後半 17.05;
舊冠軍(逐月+逐小時)是 16.99 / 18.07。**挑模型要看兩半期,不是只看總平均。**

### 1.8 怎麼讀那些指標

| 指標 | 跟誰比 | 怎麼看 |
| --- | --- | --- |
| **MAE** | — | 平均猜錯幾歐。17.60 對照電價平均 86.6 ≈ 20% |
| **RMSE** | — | 重罰大失誤。**RMSE ÷ MAE 越大 = 尖峰爆走越嚴重**(這裡約 1.6 倍) |
| **R²** | 「永遠猜測試期平均價」 | ⚠️ **很寬鬆的尺** —— naive-24h 都有 0.31 |
| **rMAE** | 「照抄昨天同一小時」 | **<1 才及格**,文獻標準尺,嚴格得多 |

🔴 **rMAE ≠ 錢**:電池線的紀錄是 rMAE 0.54 的模型吃到 93% 的套利錢 —— 套利只需要**排序**對。

### 1.9 一個查過的假警報

跑的時候會噴 `oc_*` 六欄「沒有觀測值」。是真的:**北歐 FBMC 2024-10 上線後 offered capacity
停止發布**,那六欄從 2025 年起整年全空。**但實測對 MAE 沒影響**(Lasso 去掉後 19.124 → 19.119),
線性模型自己把係數壓掉了。**不必改特徵集**,但要知道警告的來源。

---

## 2. 模型 B —— 預測不平衡價差

### 2.1 `dev` 是什麼(不是模型的東西,是兩個公開價格相減)

```
dev = 不平衡價 − 日前價
```

真實例子(DK2,2026-06-15):

| 時間 | 日前價 | 不平衡價 | dev | |
| --- | --- | --- | --- | --- |
| 10:00 | 9.93 | 9.93 | **0.00** | 系統剛好平衡 → 制度規定兩價相等 |
| 10:15 | 6.70 | 48.79 | **+42.09** | 系統缺電 |
| 12:00 | 5.20 | 1.05 | **−4.15** | 系統電太多 |

**白話:你沒照日前計畫走的話,每一度電要多付/少收多少錢。**

### 2.2 決策時點決定了能用什麼(交割日 D 的時刻表)

| 時刻 | 發生什麼 | 對哪一天 |
| --- | --- | --- |
| **D−2 12:55** | D−1 的日前價出清並公布 | D−1 |
| **D−1 12:00** | **日前投標關門** ← ★ 決策時點 | 替 D 日 96 格下單 |
| D−1 12:55 | **D 日的日前價才公布**(15 分制後由 12:45 改成 12:55) | D |
| D−1 18:00 | 🔴 ENTSO-E 日前預測的**法規截止時點** | D |
| D 交割後 | 不平衡價逐格公布 | D |

由此得到三條規矩:

1. **D 日的日前價不是特徵**,它在關門後 55 分鐘才出來 —— **它是目標的一半**。
2. **不平衡價衍生的特徵要 as-of 關門**:窗口收在關門前 1 小時(≈ D−1 11:00)。
   ⚪ **現貨價落後 1 天就合法**(D−1 的價 D−2 就公布了)。
3. 🔴 **ENTSO-E 日前預測不保證趕得上關門**(EU 543/2013 只要求 D−1 18:00 前,**晚六小時**)
   → `agent.BID_TIME_SAFE = True` 預設關掉那 7 個特徵。

### 2.3 完美預知上界(分母)

`new_src/trading/oracle.py`。純交易員 10 MW,每期押對邊,乾淨窗口(2025-12-08 起):

| | 15 分版 | 逐時版 | 15 分多賺 |
| --- | --- | --- | --- |
| DK1 | **4,433 k€/年** | 3,972 k€/年 | **+11.6%** |
| DK2 | **5,100 k€/年** | 4,638 k€/年 | **+10.0%** |

⚠️ **這是分母不是收益。** 🔑 **固定單邊策略只拿到上界的 1.2–2.5% → 價值幾乎全在「押對邊」。**

### 2.4 第一輪:骨架與兩個自己造成的錯

**v1**:19 特徵 · LightGBM 回歸 · 用 `|dev|` 當樣本權重 → DK1 −0.3% / DK2 +1.6%。

🔴 **錯誤一:`|dev|` 當樣本權重是錯的。**
±P 部位的最適決策是 `sign(E[dev|x])`,而**無權重**的平方誤差回歸估的正是 `E[dev|x]`。
加權估到的是被大尾巴拉走的東西,**符號就偏了**(dev 上調側平均比下調側大兩倍以上
→ 加權後模型幾乎永遠猜「缺電」)。**實測代價:DK2 從 8.7% 掉到 1.6%。**
🔑 **「用錢」屬於評分,不屬於損失函數。**

🔴 **錯誤二:ablation 的可用列用「各自的特徵」判定。**
抽掉再生能源那組會憑空多出 962 列(那些是缺風光預測的格),**連分母都不一樣**,
「少賺 pp」就是假的。改成一律用**全部特徵**判定。

**加上噪音底線**(按日 bootstrap 的 CI 寬度)= **12.7 / 17.8 pp** 之後,
ablation 的所有差距都在噪音內,**排不出順序**。
**最硬的證據**:特徵從 19 加到 26,DK1 0.6%→3.8%、DK2 8.7%→1.8%,**兩區反向移動**。

### 2.5 第一輪的預先註冊搜尋

`new_src/trading/agent_search.py`,**七個變體跑之前就寫死**,
選擇窗 2026-03→06 挑一個,**驗證窗 2026-06→08 只讓贏家跑一次**:

| 變體 | 針對什麼 |
| --- | --- |
| ① 基準 LightGBM 回歸 | — |
| ② Ridge 強正則化 | R²≈0 時樹可能只在學噪音 |
| ③ **+歷史預測偏差** | 新資訊:日前預測若系統性偏高,那個偏差用**落後 2 天**的資料學得到 |
| ④ +偏差 · Ridge | ②③ 合併 |
| ⑤ 拆解(方向機率 × 歷史幅度) | dev 尾巴肥,平方誤差被極端值主導 |
| ⑥ 拉長訓練(2025-03-18 起) | 資料量 |
| ⑦ +偏差 · 不確定就不進場 | 動作空間加「空手」 |

**選擇窗**:DK1 ③ +4.98%、DK2 ① **+16.16%** → **驗證窗:+1.27% / +0.61%,都掉回噪音。**

🔑 **DK2 那個 +16.16% 是這輪第一個危險品** —— CI 只差一點就離開 0,看起來很像成功。
**沒有事先切驗證窗的話,這輪會宣布 B 成功。**

### 2.6 🔑 病因診斷:訊號「不存在」,不是「會漂移」

`new_src/trading/why_not_predictable.py`。這兩種病意義完全不同:

| 病 | 意思 | 有救嗎 |
| --- | --- | --- |
| **A 訊號不存在** | 關門時看得到的東西裡沒有方向的資訊 | 換模型沒用,只能換**資訊集** |
| **B 訊號會漂移** | 資訊在,但關係逐月變化 | 有救:線上學習、短重訓、regime 偵測 |

⚠️ 「in-sample 作弊回收 93.8%」**分不出這兩種** —— 那是**記憶**不是預測。
**決定性實驗 = 同一段期間內的交叉驗證,兩種切法**:

| 切法 | DK1 R² | DK2 R² | 意義 |
| --- | --- | --- | --- |
| 隨機 5 折 | +0.17 | +0.42 | 鄰近格落到訓練集 → **偷得到局部自我相關**,寬鬆上界 |
| **連續區塊 5 折** | **−0.16** | **−0.26** | 不偷相鄰資訊 → **誠實的同期上界** |

**連續區塊版是負的** → 同期、同分佈、沒有 regime 變化的條件下都學不動 → **病 A。**
而且**幅度 `|dev|` 一樣不行**(−0.18 / −0.29),所以也不能靠「只在會貴的時候進場」救。

**機制在自我相關裡**:

| 落後 | DK1 ACF | DK2 ACF |
| --- | --- | --- |
| 15 分鐘 | **+0.372** | **+0.619** |
| 1 小時 | +0.087 | +0.200 |
| 1 天 | +0.014 | +0.013 |
| **2 天(能用的最近位置)** | **−0.0015** | **−0.0027** |

**dev 有很強的記憶,只是全部在前一小時內。** 這跟制度完全吻合:
2025-03-18 之後不平衡價由 **max(mFRR 邊際價, aFRR 加權價)** 決定,
而 aFRR 在 PICASSO 上**每 4 秒清算**,設價的量中位只有 **2.4–3.2 MW**。

**錢又高度集中**:dev 恰好為 0 佔 21.2% / 31.7%;最極端的 **1% 格佔上界 14.9% / 15.9%**,
10% 格佔 43.9% / 49.3% → **一半的錢在 10% 的格裡**。

### 2.7 🔑 訊號存在,但在關門之後才出現

`agent.horizon_probe()`(**故意 leak,只當診斷**)。用再生能源**預測誤差**押一條沒訓練的規則
(發電比隔日預期少 → 押缺電):

| 誤差看到的時點 | 相關 | 回收 |
| --- | --- | --- |
| 交割前 5 小時 | −0.07 | **+12%** |
| 交割當下 | −0.10 ~ −0.12 | **+16~17%** |

**一條沒訓練的規則,贏過全部日前特徵一個數量級。**
→ 🔑 **綁住階段 0 的是資訊集,不是模型。**

### 2.8 第二輪:修好特徵接法 + 短 rolling window

起因是使用者的兩個質問,**兩個都成立**:

1. **「不平衡價順著出來就好,為什麼砍到落後 2 天」** → 對。原本一律落後 48 小時是實作偷懶。
   改成 **as-of 關門**。⚠️ 實測差別 ≈ 0(ACF 13h +0.008 / 24h +0.013 / 48h −0.004)。
   🔴 **第一版接錯,被 leak canary 抓到**:窗口收在 12:00 那一格,但那格 **12:00–12:15 才交割**。
2. **「A 能用 rolling window,B 為什麼不試」** → 對。第一輪只測**拉長**訓練,**沒測縮短**。
   ⚪ 但 **detrend 轉移不過去**:A 的 detrend 治的是目標水準漂移,而 **dev 本身已經是個差、
   平均 ≈ 0、沒有趨勢**,沒有東西可以去掉。

**六個變體,兩區的贏家在驗證窗都沒撐住。**

### 2.9 🔴🔴 第二輪最重要的產出:**顯著性門檻原本是壞的**

DK1 的贏家在驗證窗拿到 **+3.97%,CI [+0.7, +7.5] —— 通過了原本的門檻**。

**它不是真的。** 讓我起疑的是四個矛盾訊號(不是先跑檢查才發現的):
①在**選擇窗是負的**(−1.72%),只是六個裡最不差 ②預測與目標**負相關**(−0.019)
③**DK2 完全沒有**(−4.26%) ④驗證窗已被用過一次。

查出來的原因:**按日 bootstrap 只量「抽哪些天」的不確定性,沒量「模型訓練本身」的。**
同一個變體只換 random seed,回收在 **−4.53% ~ +4.29%** 之間跳(sd 2.74pp)
—— **比那個 CI 整段還寬。**

> ✅ **門檻從此是兩條:①CI 離開 0 ②八個 seed 全部同號。缺一不算。**
> 已寫進 `agent_search.seed_spread()`。

### 2.10 第三輪:把 A 的完整特徵集給 B

使用者的提議。B 原本只有 19 個特徵,A 有 50 個(燃料價、碳價、匯率、詳細氣象、
鄰國殘餘、聯絡線容量)—— **這是真的沒補過的缺口**。

用 A 的 50 個特徵(**已排除兩個 leak**)+ BEST 設定,同一段期間、同一個管線,只換目標:

| 目標 | DK1 R² | DK2 R² |
| --- | --- | --- |
| **日前電價** | **+0.757** | **+0.745** |
| **價差 dev** | **−0.003** | **+0.001** |

四個 seed 在 dev 上有正有負(DK1 `[1.22, 2.74, 3.23, −2.30]`、DK2 `[10.11, −1.40, −4.41, 1.74]`)。

**也直接檢定了「電價越準的月份 dev 越好猜」**:DK1 相關 **+0.820**(方向相反)、DK2 −0.555
—— **兩區符號打架,不成立。**

🔑 **推論裡少的那一步**:`dev = 不平衡價 − 日前價`。**日前價猜再準,還是不知道不平衡價。**

### 2.11 結論:B 在階段 0 是死的,而且有四層獨立證據

1. 同期**連續區塊** CV 是負的(−0.16 / −0.26)
2. ACF 在唯一能用的位置(落後 2 天)= −0.002
3. **A 的完整 50 特徵集也救不了**(R² −0.003 / +0.001)
4. 關門**之後**的資訊卻能回收 12–17%

---

## 3. 三個被攔下來的假結果(方法論產出)

| 假結果 | 被誰攔下 | 如果沒攔下會怎樣 |
| --- | --- | --- |
| as-of 特徵偷看關門後那 15 分鐘 | **leak canary** | 拿一個偷看未來的模型宣布「B 有進展」 |
| 選擇窗 DK2 +16.16% | **預先註冊的 holdout** | 宣布 B 成功(驗證窗只剩 +0.61%) |
| 驗證窗 +3.97%、CI [+0.7, +7.5] | **seed 檢查(這輪才加)** | 論文裡出現一個假陽性 |

**三個都是被檢查攔的,不是被直覺攔的。** 這一輪最該保留的資產就是這三個檢查。

---

## 4. 額外的新發現(不屬於 A 或 B,但這輪查出來的)

### 4.1 不平衡價的四個制度期(完整記錄在 `DATA.md` §12)

€26 → €76 的跳升**主要不是市場變劇烈,是定價規則換了**。

| 日期 | 改了什麼 |
| --- | --- |
| **2025-03-04** | 北歐 mFRR EAM 上線 + **不平衡結算**轉 15 分鐘 |
| **2025-03-18** | **不平衡價改成同時吃 mFRR 與 aFRR** ← 🔑 **本輪從資料裡查出來的** |
| **2025-09-30** | **日前市場**轉 15 分鐘 |
| **2025-12-08** | mFRR EAM **定價缺陷的永久修正** |

DK2 分解:25.89(舊制)→ 43.00(反事實「只吃 mFRR」)→ **75.53(實際)**
→ **aFRR 單獨佔總增幅 66%**(DK1 63%)。
反推的定價規則命中 **98.5% / 99.1%**。

### 4.2 沒有白吃的午餐

「永遠押同一邊」的平均 dev,按日 bootstrap:
DK1 **−0.62** CI [−3.64, +2.43]、DK2 **+1.47** CI [−3.40, +7.16] → **兩區都不顯著**,逐月符號還會翻。

### 4.3 🔑 不平衡價的不對稱(階段 1–3 的經濟引擎)

| | DK1 | DK2 |
| --- | --- | --- |
| 系統缺電(你少發電要買回來) | 佔 32.7%,**E[dev] = +76.5** | 24.4%,**+122.1** |
| 系統電多(你多發電要賣掉) | 53.6%,−47.8 | 50.9%,−55.7 |
| **少發電的懲罰是多發電的** | **1.60 倍** | **2.19 倍** |

### 4.4 🔴 但「投標偏移」沒有用(測過的負面結果)

一座平均出力 10 MW 的再生能源電廠,投標量乘上 (1+k):

| k | DK1 年化 | DK2 年化 |
| --- | --- | --- |
| −0.20 | −219.3 k€ | −244.1 k€ |
| **0(投標 = 日前預測)** | **−196.5 k€** | **−286.8 k€** |
| +0.20 | −173.7 k€ | −329.6 k€ |

**兩區的最佳方向相反**,所有 CI 都跨 0。
**理論上也不該有用**:丹麥是**單一價格**制度,E[dev] ≈ 0,偏移的期望效果 = `−c × E[dev]` ≈ 0。

### 4.5 🔑 真正的量:**結構性不平衡成本**

| 一座平均 10 MW 的再生能源電廠,投標 = 日前預測 | 年化 |
| --- | --- |
| DK1 | **−196.5 k€/年** |
| DK2 | **−286.8 k€/年** |

為什麼偏移救不了:**自己的預測誤差與 dev 的相關是 −0.115 / −0.101**
—— **你發得比預期少的時候,全區的人也發得少,系統缺電,價格正好最貴。**
你不是偶爾倒楣,是**每次都在最貴的時候買回來**;這是結構性的,投標偏移改不了相關性。

### 4.6 資料層的坑

- 🔴 **產量實測資料只到 2026-01-06,檔名卻寫 2026-08-21** —— 「檔名 vs 實際涵蓋」,
  做階段 1 之前要先補抓(`new_src/data/coverage.py` 就是為此存在)。
- ⚪ `training` view **已經納入 15 分鐘電價**(到 2026-08-21,有 `price_is_15min_derived` 標斷點)
  —— README 舊的警告作廢。
- ⚪ `oc_*` 六欄 2025 年起全空(FBMC),**實測無影響**。
- 🔴 `DominatingDirection == 0 → 不平衡價 == 現貨價` 這條 self-check,**DK1 有 1 格違例**
  (2025-03-16 17:30),先前記的「兩區都 100%」不準。

---

## 5. 對論文的意義與下一步

### 5.1 B 失敗不影響計畫,但重心移動了

| | 原本 | 現在量到的 |
| --- | --- | --- |
| 純交易能拿多少 | 未知 | **≈ 0(已定案)** |
| 論文價值來自 | 交易技巧 + 資產 | **幾乎全部來自資產與彈性** |
| 15 分改制的意義 | 讓 agent 賺更多 | **讓上界多 10–11.6%,但要有資產才拿得到** |

🔑 **階段 0 = 0% 是一個乾淨的零點**:之後階段 1/2/3 每多一分,都能明確歸因給「多了什麼」,
不會跟交易技巧混在一起。**「RL 到什麼複雜度才值得」這個研究問題完全保住。**

### 5.2 結構性的事實:只能往資產走

能賺錢的資訊**在關門之後才出現**(5 小時前 12%、交割當下 17%)。要用它只有兩條路:

- **日內交易** → 需要 XBID 訂單簿,**付費拿不到**(已確認的死路)
- **實體彈性** → 讓你在資訊到達後還能改變行為 = **階段 1–3**

**沒有第三條。**

### 5.3 建議的問法(換一個,不需要預測任何東西)

| | 原本 | 改成 |
| --- | --- | --- |
| 問題 | agent 能**賺**多少 | **能避掉多少既有的損失** |
| 基準 | 0 | **−197 / −287 k€/年**(已實測) |
| 靠什麼 | 預測 dev(已證實不可能) | **彈性** |

而「彈性值多少」現在有**兩個獨立的上界可以互相驗證**:
資訊面 12–17% 的完美預知上界 · 成本面 −197/−287 k€/年的結構性成本。

### 5.4 明確建議不要做

- **不要再調 B**。三輪、二十個變體、四種訓練窗、兩種特徵集(19 與 50)、四種模型類別。
  病因是訊號不存在,再調只會撞多重比較。
- **不要買 XBID**。買了得到的是階段 0 的延伸,而階段 1–3 用免費資料就能做。

---

## 6. 怎麼重跑

```bash
python new_src/models/baseline.py             # A:四個模型 + ★BEST(約 5 分鐘)
python new_src/models/experiments.py          # A:完整網格 44 設定(約 2.5 小時)

python new_src/trading/imbalance_regimes.py   # 四個制度期 + 反推定價規則(約 10 秒)
python new_src/trading/oracle.py              # 階段 0 完美預知上界(約 10 秒)
python new_src/trading/why_not_predictable.py # B 為什麼學不動(約 3 分鐘)
python new_src/trading/agent.py               # B:骨架 + ablation + 兩個對照(約 20 分鐘)
python new_src/trading/agent_search.py            # B 第一輪(約 20 分鐘)
python new_src/trading/agent_search.py --round2   # B 第二輪(約 20 分鐘)
```

產出寫到 `figs/forecast/` 與 `figs/trading/`(⚠️ `figs/` 是 gitignored,重跑可重生)。
**每一支都有會炸的 self-check**,共 10 個。
