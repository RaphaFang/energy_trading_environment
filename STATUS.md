# 現況盤點(living document)

**最後更新 2026-08-09**(原名 `sumup_0806.md`)。

這份文件**只描述程式碼裡實際存在的東西**,不提改進建議、不含新實驗。
所有引用的數字都來自程式碼裡的常數,或先前對話已產生的結果(會標明)。
不確定的地方一律寫「不確定」。

> **2026-08-07 的變動**(細節見各節):
>
> - 燃料資料重整成 **raw 儲存**,新增**煤(API2)**與 **EUR/USD 匯率**;碳價改用真實 EUA 序列
> - `ef` 拆成 `ef_chp`/`ef_pb`,新增 `ARCHETYPES`(gas/biomass/coal)
> - 拿到**丹麥能源署 Technology Catalogue** → 新增 `heat/dea.py`,**可解掉 13 個佔位值**
> - 新增 `heat/fuelmix.py`(DK1 燃料組成)、`heat/calibrate.py`(熱需求校準)、`heat/flexibility.py`(彈性價值拆解)
> - **§7 的第 3 點不確定已解答**:目錄裡背壓式與抽汽式都有,必須逐機組判斷
>
> **當天稍晚(第二批)**:
>
> - **碳價改用 ICAP** → duckdb 重建,碳價覆蓋 **52% → 100%**、**煤價首次進 `training` view**
> - **`chp.Plant` 的 13 個技術參數全部換成 DEA 目錄真值**(不再是佔位值),新增 `chp.dea_plant()`
> - 🔴 **研究範圍縮到 DK2**(使用者決定,見 §0)
>
> **2026-08-08 的變動**:對 CSV 驗證了 DEA 目錄的三個結構性事實,見 **§4.7**。
> 其中兩項是**目前擋路的問題**:`eta_el` 基準用錯(bug)、LP 表達不了背壓機組(結構)。

範圍:`new_src/heat/`(現行主線)、`new_src/data/`(資料層)、`new_src/battery/`(已 park)。

## 0. 研究範圍:**只做 DK2**(2026-08-07 決定)

**理由**:DK2(大哥本哈根)有 `varmelast.dk` 的**實際逐時熱需求真值**,DK1 沒有。
先前一整批 DK1 的猜測全部是為了繞過「DK1 沒有逐時熱資料」這件事。範圍縮到 DK2 之後
這些猜測直接**不必解**(程式碼先留著不刪,之後若回頭做 DK1 再撿回來):

| 原本的佔位值/假設                                 | DK2-only 之後                                                                             |
| ------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| `ANNUAL_TWH_DK1 = 19.0`(§4.4)                     | **不需要** —— varmelast 實測 CTR+VEKS 年熱量 **8.82 TWh_th**(均 1,007 / 尖峰 2,791 MW_th) |
| `SYSTEM_SHARE = 0.08`(§4.5,「完全沒有依據」)      | **不需要** —— varmelast 的 `/api/v1/heatdata` 有廠級所有權與容量,可切出真實業者           |
| 度日代理本身                                      | **降級成備援** —— DK2 有真值,不必用代理                                                   |
| 「DK2 校準的形狀能否移轉到 DK1」(§7 第 9 點)      | **問題消失**(不再移轉)                                                                    |
| 「日內尖峰 13h 是不是傳輸層量測假象」(§7 第 8 點) | **仍要查證**,但只影響 DK2 自己的詮釋,不再是外推風險                                       |

⚠️ **代價**:DK2 是單一大型都會系統,不是多個獨立熱網 → 「多業者、相關熱需求」的核心
假說要靠 **CTR / VEKS / 各廠業主(HOFOR、Vestforbrænding、ARGO…)** 的區分來建構,
而不是靠地理上分開的城市。這個 agent 切分方式**尚未定案**。

---

## 1. 這個 agent 是誰,它在決定什麼

### 白話

一個 agent = **一個區域供熱系統(district heating system)**,也就是「一個熱網 + 它的所有產熱設備」,被當成**單一決策者**。它不是一家公司、也不是單一機組,而是「一個熱網的調度中心」這個抽象。

它手上有五種產熱方式(熱電機組、電鍋爐、熱泵、尖峰鍋爐、蓄熱槽),每小時要決定用哪幾種、各出多少,**唯一的硬性義務是每小時把熱需求填滿**。電是副產品,可以賣;電鍋爐和熱泵則是反過來買電。

**目前只有一個 agent。** 程式碼裡沒有任何多 agent 的結構(詳見第 6 節)。

### 一次解多長

`chp.solve()` 把**整段期間當成一個 LP 一次解完**,長度由呼叫端決定:

| 呼叫處               | 期間長度               |
| -------------------- | ---------------------- |
| `chp.demo()`         | 48 小時(合成價格)      |
| `chp._real_demo()`   | 744 小時(2024 年 1 月) |
| `flexibility.main()` | 8,784 小時(2024 全年)  |

因為是一次解完整段,**agent 對整段期間的電價、氣溫、熱需求都是完全預知的**(perfect foresight)。沒有滾動視窗、沒有不確定性、沒有預測誤差。

### 決策變數

每個變數都是長度 T 的向量(T = 小時數)。程式碼裡是 8 個區塊串成一個長向量,順序為 `P, Qc, Qe, Qh, Qpb, ch, dis, S`。

| 符號  | 程式碼名 | 白話意思         | 單位   | 下界 | 上界              | 上界從哪來                          |
| ----- | -------- | ---------------- | ------ | ---- | ----------------- | ----------------------------------- |
| P     | `P`      | 熱電機組發電量   | MW_e   | 0    | `p_max` = 400     | 佔位值                              |
| Q_chp | `Qc`     | 熱電機組產熱量   | MW_th  | 0    | **無上界**        | 由背壓線+容量線間接限制             |
| Q_eb  | `Qe`     | 電鍋爐產熱量     | MW_th  | 0    | `eb_max` = 100    | 佔位值                              |
| Q_hp  | `Qh`     | 熱泵產熱量       | MW_th  | 0    | `hp_max` = 50     | 佔位值                              |
| Q_pb  | `Qpb`    | 尖峰鍋爐產熱量   | MW_th  | 0    | `pb_max` = 10,000 | 佔位值,刻意設很大以保證 LP 一定有解 |
| ch    | `ch`     | 蓄熱槽充熱速率   | MW_th  | 0    | `s_rate` = 300    | 佔位值                              |
| dis   | `dis`    | 蓄熱槽放熱速率   | MW_th  | 0    | `s_rate` = 300    | 佔位值                              |
| S     | `S`      | 蓄熱槽當下儲熱量 | MWh_th | 0    | `s_max` = 3,000   | 佔位值                              |

**衍生輸出(不是決策變數,是解完之後算的):**

- `p_buy` = `Qe`/η_eb + `Qh`/COP — power-to-heat 的買電量(MW_e)
- `p_net` = `P` − `p_buy` — 對電網的淨部位(正=賣電)
- `fuel` = (`P` + Cv·`Qc`)/η_el — 燃料消耗量(MW_fuel)
- `el_net` — 電力市場淨額(€),見第 2 節
- `heat_cost_per_mwh` — 單位供熱淨成本(€/MWh_th)

---

## 2. 目標函數

### 白話

程式在**最小化「履行供熱義務的淨成本」**。

賣電收入是**抵減**(讓成本變低),買電和燒燃料是**支出**。它不是在最大化利潤,因為這個模型裡熱根本沒有收入項。

### 實際實作的式子

`chp.solve()` 用 `scipy.optimize.linprog` 最小化 `c'x`。程式碼裡的成本係數(chp.py 第 131–137 行)逐項是:

```
cost_fuel_t = fuel_price_t + co2_price × ef          (€/MWh_fuel)

c[P]   = −p_t + cost_fuel_t / η_el
c[Qc]  = cost_fuel_t × Cv / η_el
c[Qe]  = p_t / η_eb
c[Qh]  = p_t / COP_t
c[Qpb] = (fuel_price_pb_t + co2_price × ef) / η_pb
c[ch]  = 0
c[dis] = 0
c[S]   = 0
```

展開成完整的最小化目標:

```
min  Σ_t [ −p_t·P_t                                    ← 賣電收入(負號=抵減成本)
         + (f_t + c_co2·ef)·(P_t + Cv·Q_chp,t)/η_el   ← 熱電機組的燃料+碳成本
         + p_t·Q_eb,t/η_eb                             ← 電鍋爐買電
         + p_t·Q_hp,t/COP_t                            ← 熱泵買電
         + (f^pb_t + c_co2·ef)·Q_pb,t/η_pb ]           ← 尖峰鍋爐燃料+碳成本
```

等價於最大化:`Σ p_t·P_t − 燃料成本 − 買電成本`。

### 逐項解釋

| 項                         | 是什麼                 | 為什麼在裡面                                                                   | 單位 |
| -------------------------- | ---------------------- | ------------------------------------------------------------------------------ | ---- |
| `−p_t·P_t`                 | 賣電收入               | 電是熱電共生的副產品,賣掉可抵減供熱成本                                        | €    |
| `(f+c·ef)(P + Cv·Qc)/η_el` | 熱電機組的燃料與碳成本 | 燃料量 = (發電量 + 抽汽損失)/發電效率。Cv 項表示「多抽 1MW 熱要犧牲 Cv MW 電」 | €    |
| `p_t·Qe/η_eb`              | 電鍋爐買電支出         | 產 1MW_th 熱要買 1/η_eb MW_e 電。**電價為負時這項為負 = 被付錢吃電**           | €    |
| `p_t·Qh/COP_t`             | 熱泵買電支出           | 同上但除以 COP(COP 2.8 → 只要 1/2.8 的電),COP 隨氣溫變                         | €    |
| `(f^pb+c·ef)Qpb/η_pb`      | 尖峰鍋爐燃料與碳成本   | 純燒燃料產熱,不發電,是其他選項的成本天花板                                     | €    |
| `ch / dis / S` 係數為 0    | 蓄熱槽本身不花錢       | 只有機會成本(何時充放),沒有直接營運成本                                        | —    |

### 熱收入在不在裡面?

**不在。** 兩個理由:

1. **數學上不影響最適解。** 熱需求 `d_t` 是外生給定、而且是**等式約束**必須剛好滿足。所以熱收入 = 熱價 × Σd_t 是一個**常數**,加進目標函數不會改變 argmin。
2. **與丹麥制度一致。** 丹麥區域供熱受成本回收原則規範,熱不是利潤中心。所以「最小化供熱成本」本來就是正確的目標,而不是「最大化利潤」。
   ⚠️ **這一點我沒有查證過原始法規**,詳見第 7 節。

因此:

- `el_net` = −目標函數值 = 賣電 − 買電 − 燃料。**這個數字通常是負的,那不是虧損**,而是「供熱這件事的淨成本」。
- `heat_cost_per_mwh` = −`el_net` / Σd_t = **單位供熱淨成本(€/MWh_th)**,這才是可解讀、可跨情境比較的指標。

### 目標函數裡沒有的東西

- 固定成本、資本支出(CAPEX)、折舊
- 運維成本(O&M)
- 機組啟動成本
- 電力側的稅費與電網費(丹麥對 DH 的電鍋爐/熱泵有特殊稅制 —— **完全沒建模**,見第 7 節)
- 熱網輸送損失
- 蓄熱槽以外的任何機會成本

---

## 3. 約束式

### 已實作(程式碼裡真的有)

**① 熱平衡(等式,T 條)** — chp.py 第 143 行

```
Q_chp,t + Q_eb,t + Q_hp,t + Q_pb,t + dis_t − ch_t = d_t      ∀t
```

白話:每小時產出的熱(含從槽裡放出來的)扣掉存進槽裡的,必須**剛好等於**熱需求。
**防止的是**:agent 為了賺電費而不供熱,或產出用不掉的熱。這是「供熱義務」的數學形式。

**② 蓄熱槽動態(等式,T 條)** — chp.py 第 145–150 行

```
S_t − (1 − s_loss)·S_{t−1} − ch_t + dis_t = 0    ∀t ≥ 1
S_0 − ch_0 + dis_0 = (1 − s_loss)·s_init          (t = 0)
```

白話:槽裡現在的熱 = 上一小時剩下的(扣掉散熱損失)+ 這小時充的 − 這小時放的。
**防止的是**:熱憑空出現或消失。配合 `S ≥ 0`,保證「放出來的熱一定是之前充進去的」。

**③ 背壓線(不等式,T 條)** — chp.py 第 154 行

```
Cb·Q_chp,t − P_t ≤ 0      即   P_t ≥ Cb·Q_chp,t
```

白話:熱電機組產熱時,發電量不可能低於某個比例。這是熱電共生的物理現實 —— 要產熱就一定會發電。
**防止的是**:agent 把機組當成純鍋爐用(只產熱不發電),那在物理上做不到。

**④ 容量線(不等式,T 條)** — chp.py 第 155 行

```
P_t + Cv·Q_chp,t ≤ p_max
```

白話:發電量加上「為了產熱而犧牲掉的發電量」,不能超過機組的等效凝汽容量。
**防止的是**:機組出力超過實體容量。同時表達了抽汽式機組「多產熱就要少發電」的取捨。

**⑤ 變數上下界** — chp.py 第 159–169 行
所有變數 ≥ 0;上界見第 1 節的表。
**防止的是**:負的產熱量、超過設備容量的出力、槽裡存超過容量的熱。

### 尚未實作(討論過但程式碼裡沒有)

| 項目                      | 說明                                                                             |
| ------------------------- | -------------------------------------------------------------------------------- |
| 機組啟停(unit commitment) | 最小負載、啟動成本、開關機狀態。需要整數變數,**刻意第一版不做**                  |
| 爬坡限制(ramp rate)       | 機組出力每小時可變化的上限                                                       |
| 期末蓄熱條件              | 目前沒有 `S_T = S_0` 之類的條件。理由:`s_init=0` 加上 `S ≥ 0` 已保證不會憑空生熱 |
| 熱網溫度/水力             | 供水溫、回水溫、管網延遲、輸送損失全部沒有                                       |
| 價格衝擊 λ                | agent 的買賣完全不影響電價(見第 6 節)                                            |
| 多 agent 互動             | 不存在(見第 6 節)                                                                |
| 熱費率 / 熱收入           | 刻意排除(見第 2 節)                                                              |
| 電力稅費與電網費          | 丹麥對 DH 電鍋爐/熱泵有特殊稅制,**完全沒建模**                                   |
| 燃料的分種類處理          | 目前 CHP 與尖峰鍋爐共用同一個排放因子 `ef`,沒有區分生質(通常算 0 碳)與天然氣     |
| 備轉/平衡市場收入         | 只有日前電價,沒有其他市場                                                        |

---

## 4. 參數清單 ⚠️ 最重要的一節

來源分類:**(A) 真實資料** / **(B) 文獻或官方參數** / **(C) 佔位值,我隨手設的未查證** / **(D) 校準值,從資料估出來**

### 4.1 機組技術與成本參數(`chp.Plant`)

**✅ 2026-08-07:13 個技術參數已全部換成 DEA Technology Catalogue 真值**,預設 =
`09a Wood Chips extract. plant`(year=2020, est=ctrl)。`chp.dea_plant(arch, est=)` 可換
原型或取 lower/upper 區間。`chp.demo()` 有 self-check 比對預設值 vs 目錄,防手抄漂掉。

| 參數名             | 白話意思              | 目前值(木片抽汽)                                     | 來源    | 具體出處                                                                                                                            |
| ------------------ | --------------------- | ---------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `p_max`            | 熱電機組等效凝汽容量  | 258.2 MW_e                                           | **(B)** | DEA 目錄。⚠️ 是**單一機組**典型值,未與熱網規模對齊(見 §7 第 5 點)                                                                   |
| `cb`               | 背壓係數(功熱比下界)  | 0.45                                                 | **(B)** | DEA 目錄。**原佔位值 0.75 高估近一倍**;真值隨技術差 4 倍(氣 CC 1.80)                                                                |
| `cv`               | 抽汽損失係數          | 0.14                                                 | **(B)** | DEA 目錄                                                                                                                            |
| `eta_el`           | 電效率(net, **name plate**) | **0.430**                                       | **(B)** | DEA 目錄。**2026-08-10 從 0.409(annual average)改過來** —— Cb 是 name plate 基準,同組參數必須同基準,見 §4.7 ②                     |
| `ef_chp` / `ef_pb` | 排放因子              | gas 0.20 / biomass 0.0 / coal 0.34(`chp.ARCHETYPES`) | **(C)** | 量級值。**目錄裡沒有排放因子**(那是燃料屬性)→ 仍需別的來源                                                                          |
| `eb_max`           | 電鍋爐熱容量          | 20 MW_th                                             | **(B)** | DEA `41 Electric boiler, large`,單台。⚠️ 台數未定 → 規模仍待對齊                                                                    |
| `eta_eb`           | 電鍋爐效率            | 0.99                                                 | **(B)** | DEA 目錄                                                                                                                            |
| `hp_max`           | 熱泵熱容量            | 10 MW_th                                             | **(B)** | DEA `40 Comp. hp, airsource 10 MW`,單台。⚠️ 同上                                                                                    |
| `cop_ref`          | 熱泵 COP 參考值(@7°C) | 2.8                                                  | **(B)** | DEA 熱泵的「熱效率」欄就是 COP                                                                                                      |
| `pb_max`           | 尖峰鍋爐容量          | 10,000 MW_th                                         | **(C)** | **刻意不用目錄值**:`44 Natural Gas DH Only` 單台只有 5.25 MW_th,拿來當上限會讓 LP 在寒冬無解。這個變數的角色是可行性後備,不是銘牌值 |
| `eta_pb`           | 尖峰鍋爐效率          | 1.03                                                 | **(B)** | DEA 目錄(>1 因為以低位發熱值 LHV 計)                                                                                                |
| `s_max`            | 蓄熱槽容量            | 2,880 MWh_th                                         | **(B)** | DEA `141b Large TTES`(原佔位值 3,000,誤差 4%)                                                                                       |
| `s_rate`           | 蓄熱槽充放速率        | 288 MW_th                                            | **(B)** | 同上(原佔位 300)                                                                                                                    |
| `s_loss`           | 蓄熱槽每小時散熱率    | 0.0025                                               | **(B)** | 同上,目錄給 %/day ÷ 24(原佔位 0.002)                                                                                                |

🚩 **目錄裡的垃圾焚化(WtE)與秸稈機組全部是背壓式**,熱電綁死在 `P=Cb·Q` 一條線上、
沒有抽汽可行域 → 現行 `chp.solve()` 不適用,`dea.is_back_pressure()` 會擋。WtE 佔 DK1
熱電 13.2%、在 DK2 更吃重 → **車隊的有效彈性比「總容量」看起來的少**。

### 4.2 `chp.solve()` 的預設參數

| 參數名          | 白話意思       | 目前值                               | 來源                              | 具體出處                                                                                                                   |
| --------------- | -------------- | ------------------------------------ | --------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| `fuel_price`    | CHP 燃料價     | 預設 30 €/MWh_fuel                   | **(C)** 預設 / **(A)** 實際使用時 | `flexibility.py` 與 `chp._real_demo()` 都傳入真實 TTF 天然氣價(來自 duckdb `ttf_gas_eur_mwh`)→ 實際跑的是 (A)              |
| `co2_price`     | 碳價           | **真實 EUA 逐時序列**(2024 均 €65/t) | **(A)** ✅                        | 改用 **ICAP Allowance Price Explorer**(`Primary Market` 欄)→ **覆蓋率 52% → 100%**。與舊的 Yahoo `CO2.L` 重疊期 corr 0.986 |
| `fuel_price_pb` | 尖峰鍋爐燃料價 | **天然氣價**                         | **(A)** ✅                        | 所有原型的尖峰鍋爐都燒氣(`ARCHETYPES` 的 `ef_pb`)→ 煤機組也用 TTF 當 pb 燃料                                               |
| `s_init`        | 蓄熱槽初始儲熱 | 0.0 MWh_th                           | **(C)**                           | 設計選擇,不是查來的                                                                                                        |

### 4.3 熱泵 COP 函數(`chp.cop_from_temp`)

| 參數名    | 白話意思          | 目前值                              | 來源    | 具體出處                      |
| --------- | ----------------- | ----------------------------------- | ------- | ----------------------------- |
| 函數形式  | COP ∝ Carnot 效率 | `cop_ref × carnot(T)/carnot(T_ref)` | **(B)** | Carnot 比例式是教科書標準簡化 |
| `t_hot`   | 供水溫度          | 70 °C(寫死在函數裡)                 | **(C)** | 我設的                        |
| `t_ref`   | 參考氣溫          | 7.0 °C                              | **(C)** | 我設的                        |
| `cop_ref` | 參考 COP          | 2.8                                 | **(B)** | **取自 `Plant.cop_ref`,不再寫死** —— 2026-08-10 前這裡硬寫 3.2,而所有呼叫端都用預設值 → 實跑 COP 高 14.3%、熱泵買電低估 12.5%(bug) |

### 4.4 熱需求模型(`heat.demand`)

| 參數名           | 白話意思             | 目前值                        | 來源    | 具體出處                                                                             |
| ---------------- | -------------------- | ----------------------------- | ------- | ------------------------------------------------------------------------------------ |
| `T_BASE`         | 供熱起始溫度         | 16.0 °C                       | **(D)** | 對 varmelast 大哥本哈根實際逐時熱需求做網格搜尋估出(`heat/calibrate.py`,37,376 小時) |
| `BASE_SHARE`     | 與氣溫無關的基載佔比 | 0.35                          | **(D)** | 同上                                                                                 |
| `DIURNAL`        | 24 個日內形狀乘數    | 尖峰 13h=1.166、低谷 4h=0.808 | **(D)** | 同上,扣掉氣溫模型後的殘差型態中位數                                                  |
| `ANNUAL_TWH_DK1` | DK1 年區域供熱產熱量 | 19.0 TWh_th                   | **(C)** | **佔位值**。程式碼註解已標明「投入論文前必須用能源署統計替換」                       |

### 4.5 情境設定(`heat.flexibility`)

| 參數名         | 白話意思                              | 目前值 | 來源    | 具體出處          |
| -------------- | ------------------------------------- | ------ | ------- | ----------------- |
| `SYSTEM_SHARE` | 代表性 DH 系統佔 DK1 全區熱需求的比例 | 0.08   | **(C)** | 我設的,無任何依據 |

### 4.6 統計

| 來源分類          | 數量               | 說明                                                                                          |
| ----------------- | ------------------ | --------------------------------------------------------------------------------------------- |
| **(C) 佔位值**    | **約 7 個**(原 21) | `ef_chp`/`ef_pb`、`pb_max`(刻意)、`ANNUAL_TWH_DK1`、`SYSTEM_SHARE`、`s_init`、`t_hot`/`t_ref` |
| **(B) 文獻/官方** | **14 個**          | 13 個來自 Technology Catalogue + COP 的 Carnot 形式                                           |
| (D) 校準值        | 4 個               | 全部來自 varmelast DK2 校準,只涵蓋熱需求模型                                                  |
| (A) 真實資料      | **5 條時間序列**   | 電價、氣溫、天然氣價、**煤價**、**碳價**(後兩者 2026-08-07 進 duckdb)                         |

**✅ Technology Catalogue 已解掉並且已套進 `chp.Plant` 的 13 個**(全部附 lower/upper):
`p_max, cb, cv, eta_el, eb_max, eta_eb, hp_max, cop_ref, pb_max*, eta_pb, s_max, s_rate, s_loss`
(\*`pb_max` 例外,理由見 §4.1)

**❌ 仍是 (C) 的**:`ef_chp`/`ef_pb`(目錄無排放因子)、`ANNUAL_TWH_DK1`、`SYSTEM_SHARE`、
`s_init`、`t_hot`/`t_ref`。

**🔴 仍缺的資料(不是參數問題,是沒有來源)**:**生質燃料價**(木片/顆粒無國際期貨)。
先前用天然氣價當代理,跑出**負的基準供熱成本**,水準完全不可用 → 現在 `chp.py` 與
`flexibility.py` 只跑燃料價有真值的 `gas_cc` 與 `coal` 兩個原型,生質欄直接不列。
**但生質是丹麥熱電最大燃料(DK1 2025 佔 32.8%)** → 這是目前最痛的資料缺口。
候選來源:能源署 `Samfundsøkonomiske beregningsforudsætninger`、Energipriser 統計。

**誠實結論(2026-08-07 更新)**:機組的**技術**參數已經是官方目錄真值,不再是我隨手設的。
仍不可引用的是**成本水準**——因為①生質沒有燃料價②機組容量與熱網規模沒對齊
(DK1 2024-01 試算:氣 CC 機組 300 MW_e / 熱網均 174 MW_th = 1.7×,煤機組 2.9×,
多出來的容量當純凝汽電廠賣電,利潤被記進「供熱成本」→ 煤那欄跑出 −€16/MWh_th)。

### 4.7 🔴 DEA 目錄的三個結構性事實(2026-08-08 對 CSV 驗證)

**① 背壓與抽汽的燃料式不同源 —— 需要兩個類別,不是一個類別加開關**

抽汽表**完全沒有熱效率欄**(查 8 張全部 0 列)。這不是缺漏,是參數化方式不同:

```
背壓  F = P/η_el ;  Q = P·η_th/η_el              熱電綁死在 P = Cb·Q 一條線
抽汽  F = (P+Cv·Q)/η_el ; P ≥ Cb·Q ; P+Cv·Q ≤ P_max   有可行域面積
```

`Cv = 1.0` 是背壓表的 **N/A 哨兵值**,不是物理量(目錄註解寫明 Cv 對背壓/ORC 不存在)。
所以「Cv=1 讓可行域退化成一條線」是誤讀 —— 退化成線是對的,但原因是背壓機**本來就**
`P = Cb·Q`,與 Cv 的數值無關。

**規模**:目錄 33 張 CHP 表,**24 張背壓、只有 9 張抽汽**(2026-08-09 對 CSV 重數,確認)。
`chp.solve()` 現在只涵蓋 9 張。抽汽那 9 張是:`01 Coal CHP`、`02 LTE existing plant`、
`03a–03d`(煤改生質四張)、`05 Gas turb. CC, steam extract.`、`09a/09b Wood extract. plant`。

**DK2 實測**(varmelast 2023–2025 完整年,分來源**生產**佔比):

| 來源                                            | 佔比                        |
| ----------------------------------------------- | --------------------------- |
| `BE-VL-KRAFTV-EF` 熱電                          | **64.6%**                   |
| **`BE-VL-AFFALD-EF` 垃圾焚化**                  | **27.7%**(目錄裡全部是背壓) |
| `BE-VL-SPIDS-GAS-EF` 尖峰氣                     | 4.0%                        |
| `BE-VL-IO-EF` 工業餘熱                          | 1.0%                        |
| `BE-VL-EVO-EF` 電鍋爐 / `BE-VL-VP-EF` 熱泵      | 0.8% / 0.6%                 |
| 其餘(尖峰油/生質、沼氣、資料中心餘熱、太陽能熱) | 1.3%                        |

→ **超過四分之一的 DK2 熱量目前結構上表達不了。**

🔴 **2026-08-09 修正**:先前這裡寫 64.4 / 27.3 / 4.5,是把 **`BE-VL-TOTAL-FAK` 誤當熱量欄**
加進分母所致 —— 該欄的官方 title 是 **`CO2 - Udledning`、單位 `Kg/GJ`**(排放強度,不是 MW_th)。
上表是排除它之後、對 11 個真正的生產分項重算的。焚化佔比比原記錄更高,背壓阻塞更嚴重。
另 `LOCAL`(Lokal produktion)全期恆為 0,沒有資料。

**② `Cb` 是用 name plate 效率算的,不是 annual average —— `Plant.eta_el` 混了基準(bug)**

| 機組        | η_el/η_th (name plate) | (annual average) | 表列 `Cb` |
| ----------- | ---------------------- | ---------------- | --------- |
| 木片 Large  | **0.3489**             | 0.3258           | 0.35      |
| WtE Large   | **0.2826**             | 0.2648           | 0.28      |
| 木片 Medium | **0.3658**             | 0.3412           | 0.37      |
| 秸稈 Large  | **0.4289**             | 0.3989           | 0.43      |
| WtE Medium  | **0.2951**             | 0.2763           | 0.30      |

5 張表 5 中,annual average 全部對不上。`Cb`/`Cv`/`η_el` 出現在同一組可行域與燃料式裡,
**基準必須一致** → `Plant.eta_el` 要改用 name plate:

`wood_chips` 0.409 → **0.43** / `wood_pellets` 0.425 → **0.447** / `gas_cc` 0.56 → **0.59** /
`coal` 0.485(該表只有 name plate,現行 fallback 誤打誤撞已正確)。

⚠️ **代價**:name plate 是設計點效率,而 LP **沒有建強迫停機與最小負載** → 系統性樂觀。
目錄有 `Availability` / `Forced outage` / `Minimum load`(木片抽汽:forced 0.03、min load 0.45)
→ 用 availability 當**容量折減**補回來,不必動整數變數。

**③ 溫度基準有三種,不能混用**

大型機組是 `Cb (50°C/100°C)`,**Medium 機組是 `Cb (40°C/80°C)`**(低溫網假設),
另有一種 `(50°C/100°C, additional to existing plant)`。CTR 是高溫傳輸網
→ DK2 只能用 50/100 那批,**混進 Medium 等於偷偷改了熱網溫度**,論文裡要明講取捨。

---

---

## 5. 資料清單

### 5.1 現行熱側路線實際用到的

| 檔案/模組                    | 來源與 endpoint                            | 時間範圍與頻率               | 為什麼拉它                              | 還有沒有在用                                                                      |
| ---------------------------- | ------------------------------------------ | ---------------------------- | --------------------------------------- | --------------------------------------------------------------------------------- |
| `data/elspot_price.py`       | Energinet `Elspotprices`                   | 2019-01-01 → 2026-07-08,逐時 | 電價,LP 的價格訊號                      | ✅ 用(經 duckdb `y_price_eur`)                                                    |
| `data/weather_forecast.py`   | Open-Meteo `historical-forecast-api`       | 2019 → 2026,逐時             | 氣溫,熱需求的驅動變數                   | ✅ 用(經 duckdb `temperature_2m`)                                                 |
| `data/fuel_prices.py`        | yfinance:TTF=F、MTF=F、EURUSD=X;ICAP:EUA   | 2019-01-01 → 2025-10-01,日   | 燃料與碳的邊際成本                      | ✅ 氣/煤/碳都已接進 LP(碳價 100% 覆蓋)                                            |
| `data/varmelast_heat.py`     | varmelast.dk `/api/v1/heatdata/historical` | 2021-03 → 2026-07,逐時       | DK2 實際熱需求,用來校準度日代理         | ✅ 用(`heat/calibrate.py`)                                                        |
| `data/production_by_fuel.py` | Energinet `ElectricityBalanceNonv`         | 2019-01-01 → 2026-07-08,逐時 | 分燃料出力,原本想當熱需求代理的驗證標的 | 🟡 **已知該用途無效**(見下),目前只在 `demand.validate_against_chp()` 當敘述性對照 |
| `data/load_duckdb.py`        | 合併上述來源                               | —                            | 建 `energy.duckdb` 的 `training` view   | ✅ 用(熱側只讀 3 欄)                                                              |

**熱側實際只從 duckdb 讀三個欄位**:`y_price_eur`、`temperature_2m`、`ttf_gas_eur_mwh`。

### 5.2 電池時期留下、熱側完全沒用到的

我用 grep 確認過:`new_src/heat/` 底下**沒有任何一行**引用 `battery/`、`v2_multi`、`fringe` 或 λ。

| 檔案/模組                                  | 原本用途                             | 現在狀態                                            |
| ------------------------------------------ | ------------------------------------ | --------------------------------------------------- |
| `battery/v1_single.py`                     | 單顆電池 LP(perfect/naive)           | 🔴 熱側未用                                         |
| `battery/v2_multi.py`                      | 多 agent + λ 價格衝擊 + 輪流最佳反應 | 🔴 熱側未用                                         |
| `battery/v3_cournot.py`                    | Cournot 自制、卡特爾/競爭三把尺      | 🔴 熱側未用                                         |
| `battery/v4_wind.py`                       | 風力情境                             | 🔴 熱側未用                                         |
| `battery/fringe.py`                        | λ 估計、保序迴歸 fringe 曲線、IV     | 🔴 熱側未用                                         |
| `battery/experiment.py`                    | 電池線的實驗總入口                   | 🔴 熱側未用                                         |
| `battery/compare.py`                       | 電池 vs 統計模型的統一比較表         | 🔴 熱側未用                                         |
| `models/forecast.py`、`models/baseline.py` | 電價預測(LightGBM/Ridge/Lasso)       | 🔴 熱側未用                                         |
| `data/energinet_forecast.py`               | 風光隔日出力預測                     | 🟡 間接:進了 duckdb,但熱側沒讀                      |
| `data/residual_demand.py`                  | 殘餘負載(λ 估計用)                   | 🟡 間接:進了 duckdb,熱側沒讀                        |
| `data/entsoe_features.py`                  | 鄰國負載/風光/邊界容量               | 🟡 間接:進了 duckdb,熱側沒讀                        |
| `data/calendar_features.py`                | 日曆特徵 + spine                     | 🟡 間接:是 duckdb 的時間骨架,熱側靠它才有連續時間軸 |

⚠️ 這些不是「壞掉的程式碼」,是**已 park 的研究線**。電池線的結論(DK1 是 price-taking 價區、λ≈0.005–0.009)本身是有效的研究產出。

### 5.3 已知的資料儲存問題

- `data/varmelast_heat.py` 抓取時有 5 個季度因連線中斷失敗(2021 Q1/Q3、2022 Q1/Q3/Q4),**檔案裡沒有任何標記**說這些期間是「沒抓到」而非「本來就沒資料」。2023 起完整。
- `data/varmelast_heat.py` 沒有保留 API 回傳的 `valueError` 欄位(抽樣一週檢查全為 0,但未保留)。
- `data/production_by_fuel.py` 使用 `to_numeric(errors="coerce")`,無法解析的值會靜默變 NaN。
- `data/residual_demand.py`(電池時期,非我所寫)存的是**衍生欄** `residual = load − wind − solar`,不是純 raw。
- 好消息:**清理都在分析時做,不在儲存時做**。`calibrate.py` 是在讀取時才丟掉 0 值與官方公告的錯誤區間(2026-07-10~29),parquet 本身未被修改。

---

## 6. 市場假設

### 6a. 目前的 LP 是把電價當外生給定的歷史序列嗎?

**是。**

`chp.solve()` 接收 `price` 參數,它是一個固定的 numpy 陣列。所有呼叫端傳的都是 duckdb 裡的 `y_price_eur`(Energinet 實際歷史出清價)。價格在整個最佳化過程中完全不變,agent 的任何決策都不會影響它。

而且因為整段期間一次解完,**agent 對未來所有價格是完全預知的**。

### 6b. λ 現在有沒有被使用?值是多少?

**完全沒有被使用。**

`new_src/heat/` 底下沒有任何 λ、價格衝擊、或 `solve_day` 的引用(已用 grep 確認)。目標函數裡的價格項是 `−p_t·P_t`,是線性的,沒有任何 `λ × 自己的量` 這種二次項或推移項。

`λ = 0.004` 這個值存在於 `battery/experiment.py` 的常數 `LAM`,但**熱側從未 import 它**。

### 6c. 那等於假設 agent 是純 price-taker(λ=0)。跟原本的電池模型在「市場互動」這一層,結構有什麼不同?

**誠實回答:在市場互動這一層,目前沒有不同 —— 而且比電池線當時做到的還少。**

具體比較:

| 層次                                           | 電池線                             | 熱側現況                            |
| ---------------------------------------------- | ---------------------------------- | ----------------------------------- |
| v1 級(單 agent、price-taker、歷史價、完全預知) | `battery/v1_single.perfect`        | **`heat/chp.solve()` 就停在這一層** |
| v2 級(多 agent、λ 價格衝擊、輪流最佳反應)      | `battery/v2_multi.solve_day` ✅ 有 | ❌ 無                               |
| v3 級(Cournot 內化自身衝擊、卡特爾/競爭三把尺) | `battery/v3_cournot` ✅ 有         | ❌ 無                               |

所以:**熱側目前的市場互動結構,等同於電池線的 v1**,而電池線後來做到了 v3。在「多 agent 互動」這個面向上,熱側現在是**從零開始、還沒起步**。

**真正不同的地方在 agent 的內部結構,不在市場層**:電池 agent 只有一個狀態變數(SoC)和買/賣兩個動作;CHP agent 有熱義務等式約束、熱電聯產的可行域(背壓線+容量線)、五種產熱技術的切換、蓄熱槽、以及隨氣溫變動的 COP。這是實質的豐富化,但它發生在**單一 agent 的最佳化問題內部**,不是市場互動層。

### 6d. 「由供給側由下而上建反事實價格」實作到什麼程度?

**完全沒有開始(0%)。**

當初的構想是:技術參數 + 燃料成本 + 熱約束 → 建出每個機組的供給曲線 → 模擬競爭出清 → 得到反事實價格 → 與實際價格比較。

目前程式碼裡:

- ✅ 有:單一 DH 系統的成本型排程 LP(這是那條路的**第一塊積木**)
- ❌ 沒有:把邊際成本轉成投標曲線的程式
- ❌ 沒有:多個機組/業者的供給曲線加總
- ❌ 沒有:市場出清機制(找價格讓供給=需求)
- ❌ 沒有:任何反事實價格的產出
- ❌ 沒有:反事實價 vs 實際價的比較

模型現在仍然是**吃歷史價格**,跟當初想擺脫的做法在「價格從哪來」這件事上**沒有差別**。差別只在於它不再有 `+λ×自己的量` 這一項(因為根本沒有 λ),所以嚴格說是「更純粹的 price-taker」,不是「反事實價格」。

### 6e. 目前有幾個 agent?多 agent 互動層存不存在?

**只有一個 agent。多 agent 互動層完全不存在。**

`chp.solve()` 的簽名是單一 `plant: Plant`,回傳單一系統的排程。`flexibility.py` 雖然跑了 6 個情境,但那是**同一個 agent 的 6 種設備配置**(拿掉蓄熱槽、拿掉電鍋爐…),用來拆解彈性價值,**不是 6 個 agent 在互動**。

程式碼裡沒有:多家業者的清單、輪流最佳反應迴圈、共用的出清價格、任何一家的決策影響另一家的機制。

核心研究假說(多個 DH 業者的熱需求相關 → 彈性同時被吃掉)**目前無法用現有程式碼檢驗**,因為需要多 agent 層。

---

## 7. 我不確定的事

寫這份文件時發現以下地方我說不清楚或需要查證:

**制度與法規**

1. **丹麥熱供應法的成本回收原則(hvile-i-sig-selv)具體適用範圍** —— 我在第 2 節用它當「熱收入不進目標函數」的理由,但**我沒有查證原始法規**。特別不確定:它規範的是熱網公司還是發電廠業主?近年有無改革允許受規管報酬?這直接影響「市場力」框架是否成立。
2. **丹麥對區域供熱電鍋爐/熱泵的電力稅費與電網費** —— 我知道存在特殊稅制,但不知道細節,而且**模型完全沒建**。這可能是一階的遺漏,會改變 power-to-heat 的經濟性排序。

**參數與模型設定**

3. ~~**抽汽式是不是正確原型**~~ → **✅ 2026-08-07 已解答**:Technology Catalogue 裡**兩種都有**(例:`09a Wood Chips, Large` 是背壓、`09a Wood Chips extract. plant` 是抽汽),必須**逐機組判斷**。背壓式是 `P = Cb·Q` 一條線(熱電綁死、無彈性),抽汽式才有可行域面積。目錄用 `Cv = 1.0` 當背壓式的哨兵值並附註說明。`heat/dea.py` 的 `is_back_pressure()` 會偵測並擋下,避免給背壓機組不存在的彈性。**仍不確定的是:DK1 實際機組裡兩者各佔多少。**
4. ~~**`ef = 0.20` 對所有燃料一視同仁**~~ → **✅ 已修**(2026-08-06 拆成 `ef_chp`/`ef_pb` + `ARCHETYPES`)。**但排放因子本身仍是 (C) 佔位值** —— Technology Catalogue 裡沒有排放因子(那是燃料屬性)。
5. **🔴 機組容量與熱網規模沒有對齊(現在是最大的未解問題)** —— DEA 目錄給的是「單一機組典型值」:氣 CC 300 MW_e、煤 500 MW_e、木片 258 MW_e,而試算用的熱網平均只有 174 MW_th(比值 1.7×–2.9×)。多出來的容量當純凝汽電廠在賣電,利潤被記進「供熱成本」→ **煤原型跑出 −€16/MWh_th 的負成本**。這不是燃料價或碳價的問題。DK2 有 varmelast 的實際熱需求與廠級容量 → **這個問題在 DK2 範圍內是可以真的解掉的**,不必再猜。
6. ~~**`SYSTEM_SHARE = 0.08`**~~ → **DK2-only 後不需要**(見 §0):varmelast 有廠級所有權與容量。
7. ~~**`ANNUAL_TWH_DK1 = 19.0`**~~ → **DK2-only 後不需要**(見 §0):varmelast 實測 8.82 TWh_th。

**資料**

8. **熱需求日內尖峰在 13h 的成因** —— 我推測與傳輸層量測(管網熱慣性、傳輸延遲)和商辦負載佔比有關,但**沒有查證**。若這是量測假象而非真實需求型態,把它移轉到 DK1 會有問題。
9. ~~**DK2 校準的形狀能否移轉到 DK1**~~ → **DK2-only 後問題消失**(不再移轉)。反過來新增一個問題:**DK2 是單一大型都會系統**,「多個獨立業者的熱需求高度相關」這個核心假說要改用 CTR/VEKS/廠級業主來建構,切分方式未定案(見 §0)。
10. **varmelast 的 5 個缺漏季度** —— 檔案裡沒記錄,目前無法從檔案本身分辨「沒抓到」與「本來沒有」。
11. **`heat_cost_per_mwh` 是否可與公開的丹麥 DH 生產成本比較** —— 我先前引用過「丹麥 DH 生產成本量級 €20–50」,那是我的一般認知,**沒有查證出處**,不應該當作驗證依據。

**研究設計**

12. **這個 agent 抽象對應到現實中的哪個法人實體** —— 熱網公司?電廠業主?兩者的組合?這決定了它的目標函數該是成本最小化還是利潤最大化,也決定「市場力」是不是一個有意義的問題。目前程式碼隱含假設是「一個統管產熱與售電的單一決策者」,**但現實中這兩者可能分屬不同公司**。
