# 交接入口 —— **開新對話先讀這一頁,只讀這一頁**

**最後更新 2026-08-28。**

這一頁的用途只有一個:**告訴你該讀哪一份,以及哪些已經不算數了。**
它刻意不重複別處的數字 —— 需要實質內容時,照下面的指引去讀那一份。

---

## 現在在做什麼

**2026-08-27 轉向:交易 agent 線。** 主題是

> **在一個看不到對手、不知道對手有幾個的市場裡,一個 price-taking 的交易 agent 能拿到多少?**
> **而 2025-10-01 的日前市場 15 分鐘改制,把這個答案改變了多少?**

**沒有 λ、沒有對手模型、沒有賽局。** 有的是真實的規則改變、真實的價格、一個資料定義的上界。

### 為什麼從「多 agent 競爭」轉過來(這個結論不要重推)

**價格外生 + 凸問題 + price taker = 唯一的互動管道是 λ,而 λ 量出來是 0.005。**
電池線四種方法(OLS 控制階梯、IV、保序迴歸、隔日預測殘餘)全部收斂到 0.005–0.009。
**那不是估不準,是結構性結果:丹麥是被互聯吸收的小價區。**
→ 要做 agent 互動,只有兩條路:**建市場出清模型**(大工程)或**把耦合搬到非價格管道**。
→ 使用者選了第三條:**放掉互動,做單體 agent 在資訊貧乏下的決策**。

### 四個階段(⚠️ **這是舊版,使用者已提議改,見下一節**)

|       | agent                               | 買/賣    | 不確定性   | 彈性 | RL          |
| ----- | ----------------------------------- | -------- | ---------- | ---- | ----------- |
| **0** | 純交易員 10 MW                      | 兩邊     | 只有價格   | ❌   | ❌ 沒有位置 |
| **1** | 太陽 → 再換風(賣方)                 | 賣       | 自己的產出 | ❌   | ⚪ 邊際     |
| **2** | 有 P2H 的熱網(買方)                 | 買       | 熱需求     | ✅   | ✅ 正當     |
| **3** | CHP + P2H 組合(泛稱,參數取自 HOFOR) | **兩邊** | 兩邊都有   | ✅   | ✅          |

🔑 **RL 的研究問題本身就是「它到什麼複雜度才開始值得」** —— 每一階都跑 RL vs 最佳化基準,量那個差距什麼時候從 0 變正。

### 🔴🔴 未決:階梯要不要改(**使用者尚未拍板,不要自己決定**)

使用者 2026-08-28 提出的階梯是 **沒有電廠 → 有電廠 → 光風電 → 熱水場**,
比 repo 現在寫的多一階「**有電廠(可調度)**」。

🔑 **為什麼那一階值得插進來**:現行階梯從階段 0 直接跳到光風電,
**一次加了「不確定性」卻沒有加「彈性」**;而這一輪量到錢是在**彈性**裡
(關門後的資訊值 12–17%)。兩件事該分開測:

| | 關門後能改出力? | 自己產出有不確定性? |
| --- | --- | --- |
| 沒有電廠 | ❌ | ❌ |
| **有電廠(可調度)** | ✅ | ❌ |
| 光風電 | ❌(只能棄電) | ✅ |
| 熱水場(P2H+蓄熱) | ✅ | ✅ |

🔴 **但「有電廠」那階有一個框架問題要使用者選**:它的彈性拿來做什麼?
repo 的「明確不做」有一條是**不做 mFRR 與備轉市場**,而可調度電廠關門後的正常變現管道就是備轉。

| 選項 | 內容 | 代價 |
| --- | --- | --- |
| A | 解禁備轉(至少 mFRR) | 推翻已拍板的「不做」,要多抓資料 |
| **B(我的建議)** | 只做**日前自我排程**(價格 > 邊際成本才發),彈性當**選擇權價值**衡量但不行使 | 乾淨、資料全現成(`ept_fleet` 逐台效率 + `dea` + 燃料價),但那階較薄 |
| C | 「有電廠」直接定義成有蓄熱的電廠 | 等於少一階 |

⚠️ **刻意讓自己不平衡去賺 dev 在監管上是灰色的**(單一價格制度的設計目的就是嚇阻它),
口試會被問。熱水場那階沒有這個問題 —— 它的彈性是用來**避免**成本、正當。

### 🔴 硬約束:乾淨制度窗口只有 8.6 個月,冬季只有 83 天

| 資料源 | 實際涵蓋到 |
| --- | --- |
| 不平衡價(15 分) | 2026-08-26 |
| 日前價 15 分 · 風光預測 · 熱需求 | 2026-08-20 |
| 🔴 **產量實測** | **2026-01-06**(檔名寫 2026-08-21,要補抓) |

**乾淨窗口 2025-12-08 → 2026-08-26 = 262 天,其中冬季只有 83 天 → 不到一個完整供暖季。**

✅ **對策:把宣稱拆開。** 熱側建模可用 2021–2026 全期(varmelast 完整);
**只有「與不平衡價互動」那部分**受 8.6 個月限制。兩種宣稱不要寫在同一句話裡。

### 🔴 下一步(照這個順序,每一步都有止損點)

| 順序 | 做什麼 | 🔴 止損 |
| --- | --- | --- |
| 0 | 補抓產量實測 + 跑 `coverage.py` | 補不到就先做 1 |
| 1 | 光風電那階當**校準**:量準結構性成本,跟 arXiv 2510.16021 對答案 | 差一個數量級 = 自己錯了 |
| **2** | 🔑 **決定性實驗:完美預知 LP vs 用模型 A 預測的 LP,量那個差距** | **差距 <10% → 不要投 RL** |
| 3 | 差距夠大才做 RL vs MPC | — |
| 4 | 每一階都跑 15 分 / 逐時兩版 | — |

🔴 **第 2 步是整條路的關卡,而且很便宜。**
電池線已經量過一個很像的東西:**rMAE 0.54 的預測吃到完美預知 93% 的錢**
(見 `BATTERY_TRACK.md` §5.2)。如果熱蓄能也是這樣,**RL 的加值 ≈ 0**。
**先量那個差距,再決定要不要蓋 RL** —— 不要花三個月做完才發現它只值 3%。

### ✅ 上一輪(2026-08-28)的結果 —— **細節全部在 [`FINDINGS_2026-08-28.md`](FINDINGS_2026-08-28.md)**

這一頁只留結論。要看「怎麼做的、參數、為什麼相信」去讀那一份(477 行,六章)。

| | 結果 |
| --- | --- |
| **模型 A(預測日前電價)** | ✅ rMAE **0.66/0.72 → 0.58/0.58**、R² **0.754/0.736**。設定 = 三模型平均 · 最近 12 個月 · detrend,已接進 `forecast.BEST` |
| **模型 B(預測不平衡價差)** | 🔴 **三輪、二十個變體、兩種特徵集(19 與 50)全部 ≈ 0** |
| **階段 0 的完美預知上界** | DK1 4,433 / DK2 5,100 k€/年(10 MW),15 分比逐時 **+11.6% / +10.0%** |

**五個必須帶著走的結論:**

1. 🔴 **不平衡價是四個制度期不是一條序列**(2025-03-04 / 03-18 / 09-30 / 12-08)。
   €26→€76 主要是 **aFRR 進了定價規則**,不是市場變劇烈。**乾淨窗口 = 2025-12-08 起。**
2. 🔴 **B 學不動的病因是「訊號不存在」,不是漂移** —— 同期連續區塊 CV 是負的;
   dev 的記憶一小時就衰減完,到日前能用的位置 ACF = −0.002。**四層獨立證據。**
3. 🔑 **訊號存在,但在關門之後才出現**:交割前 5 小時 12%、交割當下 17%。
   **綁住階段 0 的是資訊集,不是模型。**
4. 🔑 **2025-10 改制在電價預測上也留下結構斷點** —— 樹模型不能外推價格水準,
   逐月重訓救不了(偏誤 −23.3),要 **rolling window + detrend** 才行。
5. 🔴 **三個假結果被三個檢查攔下**(leak canary / 預先註冊 holdout / **seed 檢查**)。
   ✅ **顯著性門檻是兩條:CI 離開 0 **且** 八個 seed 同號。**

### 📌 對照組(論文要拿來對答案的)

arXiv 2510.16021(DK1/DK2/SE3/SE4,2021–2024,太陽光電日內交易 RL):
10 MW 太陽電廠實得 **€19.9k/年**、**DK1 回收 oracle 的 38%、DK2 30%**。
→ 階段 1 一接上太陽就跟它對答案,**差一個數量級就是自己程式錯了**。

---

## 版控狀態(2026-08-28)

分支 **`feat/trading-agent-track`**(2026-08-28 從 `feat/heat-chp-track` 改名並推上 origin)。

```
e6f566f docs: write up the trading track's first full round
4105938 fix: remove two gate-time leaks from the price model, and confirm A's model still cannot predict B's target
86b993c feat: align features to gate closure, and require seed stability before believing a result
c31138c docs: align the recorded model numbers with the current test window
f8dfc4d feat: establish why the day-ahead agent cannot predict the imbalance spread
cb71aa7 feat: a much better day-ahead price model, and the regime break that hid it
```

⚠️ **舊的 `origin/feat/heat-chp-track` 還在遠端**,停在改名前的 `78ba4b8`。
要清:`git push origin --delete feat/heat-chp-track`(**使用者尚未決定,不要自己刪**)。
⚠️ **`figs/` 是 gitignored** → 實驗報告與逐時預測 CSV 不進版控,重跑可重生。
要不要開例外**使用者尚未決定**。
⚠️ `requirements.txt` 在 `battery-track-final` tag 裡(2026-08-27 刪掉的 12 個檔都在)。

## 該讀哪一份

| 文件                                         | 讀它幹嘛                                                    | 狀態            |
| -------------------------------------------- | ----------------------------------------------------------- | --------------- |
| **本頁**                                     | 入口                                                        | ✅              |
| [`FINDINGS_2026-08-28.md`](FINDINGS_2026-08-28.md) | 🆕 **交易線第一個完整回合的實驗紀錄** —— A/B 各做了什麼、參數、踩到什麼、三個被攔下的假結果 | ✅ 現行 |
| [`DATA.md`](DATA.md)                         | **資料源、欄位、坑**。要碰資料一定先讀                      | ✅ 現行         |
| [`THESIS_DIRECTION.md`](THESIS_DIRECTION.md) | 熱側主線:研究問題、政策背景、基準數字、**§10 撤回過的說法** | ✅ 現行(熱線)   |
| [`MODEL_2035.md`](MODEL_2035.md)             | 熱側的 8-agent 聯合 LP 與 2035 情境,完整記錄                | ✅ 現行(熱線)   |
| [`STATUS.md`](STATUS.md)                     | ⚠️ **只剩 §4 參數清單 / §7 / §8 / §9**,其餘已移入 `archive/`     | 🟡 部分過時     |
| [`README.md`](README.md)                     | repo 說明、結構、怎麼跑、工作慣例、已知的坑(已瘦身到 112 行) | ✅ 現行         |
| [`BATTERY_TRACK.md`](BATTERY_TRACK.md)       | 🔑 **交易線的前情提要** —— λ 為什麼是 0、預測管道長什麼樣   | 🟡 已結案但有用 |
| `archive/LITERATURE_battery.md`             | 🔴 **整份是電池線的定義,待重寫**                            | 🔴 過時         |
| `archive/`                                   | 封存,不再維護                                               | ⚪              |

## 交易線的腳本(全部從專案根目錄跑,各自有 self-check)

```bash
python new_src/trading/imbalance_regimes.py   # 四個制度期 + 反推定價規則(約 5 秒)
python new_src/trading/oracle.py              # 階段 0 完美預知上界,兩種解析度(約 5 秒)
python new_src/trading/agent.py               # 日前 agent + 特徵 ablation(約 10 分鐘)
```

產出寫到 `figs/trading/`。**`agent.py` 的 self-check 是一個 leak canary**:
把日前關門之後的價格全部挖空,那一天的 19 個特徵必須一格不變。

---

## 🅿️ 明確不做(不要再提)

**λ / 內生價格 / 賽局均衡 · 市場競爭當論文主軸 · 連續日內交易(需 XBID 訂單簿,付費拿不到)
· 內生投資決策 · LOLE · 機組啟停 · 屋頂光電小戶(需先建模聚合商層)**

⚠️ **「mFRR 與備轉市場」這一條現在有例外待決** —— 見上面「未決:階梯要不要改」的選項 A。
🅿️ **另外新增:不要再調模型 B。** 三輪、二十個變體、四種訓練窗、兩種特徵集、四種模型類別,
病因是訊號不存在,再調只會撞多重比較。
🅿️ **不要買 XBID** —— 買了得到的是階段 0 的延伸,而階段 1–3 用免費資料就能做。

## 工作慣例

- **抓資料窗口一律 2019-01-01 → 今天**(`new_src/data/window.py`)
- **舊檔一定要退場**,`retire_superseded()` 先驗證再刪 —— 寧願炸也不要靜默拿到舊檔
- **raw 永不合併**:15 分鐘的存 15 分鐘,要逐時在分析層 `resample('1h').mean()`(價格是強度量)
- **天氣與發電特徵只准用 day-ahead 預測**,短前置欄位會 leak
- **不要編數字**;巧合不是證據;寧願被告知缺什麼資料
- Energinet 的 **429 是 IP 級冷卻**,退避要 30s–600s,而且**先抓再探索**
