# 交接入口 —— **開新對話先讀這一頁,只讀這一頁**

**最後更新 2026-08-28。**

這一頁的用途只有一個:**告訴你該讀哪一份,以及哪些已經不算數了。**
它刻意不重複別處的數字 —— 需要實質內容時,照下面的指引去讀那一份。

---

## 現在在做什麼

**2026-08-27 轉向:交易 agent 線。** 主題是

> **在一個看不到對手、不知道對手有幾個的市場裡,一個 price-taking 的交易 agent 能拿到多少?**
> **而 2025-10-01 的日前市場 15 分鐘改制,把這個答案改變了多少?**

**沒有 λ、沒有對手模型、沒有賽局。** 有的是真實的規則改變、真實的價格、一個資料定義的上界。

### 為什麼從「多 agent 競爭」轉過來(這個結論不要重推)

**價格外生 + 凸問題 + price taker = 唯一的互動管道是 λ,而 λ 量出來是 0.005。**
電池線四種方法(OLS 控制階梯、IV、保序迴歸、隔日預測殘餘)全部收斂到 0.005–0.009。
**那不是估不準,是結構性結果:丹麥是被互聯吸收的小價區。**
→ 要做 agent 互動,只有兩條路:**建市場出清模型**(大工程)或**把耦合搬到非價格管道**。
→ 使用者選了第三條:**放掉互動,做單體 agent 在資訊貧乏下的決策**。

### 四個階段(由簡到繁,RL 的正當性逐階上升)

|       | agent                               | 買/賣    | 不確定性   | 彈性 | RL          |
| ----- | ----------------------------------- | -------- | ---------- | ---- | ----------- |
| **0** | 純交易員 10 MW                      | 兩邊     | 只有價格   | ❌   | ❌ 沒有位置 |
| **1** | 太陽 → 再換風(賣方)                 | 賣       | 自己的產出 | ❌   | ⚪ 邊際     |
| **2** | 有 P2H 的熱網(買方)                 | 買       | 熱需求     | ✅   | ✅ 正當     |
| **3** | CHP + P2H 組合(泛稱,參數取自 HOFOR) | **兩邊** | 兩邊都有   | ✅   | ✅          |

🔑 **RL 的研究問題本身就是「它到什麼複雜度才開始值得」** —— 每一階都跑 RL vs 最佳化基準,量那個差距什麼時候從 0 變正。

### 🔴 下一步(照這個順序)

1. **階段 1(賣方)**:接太陽,對照 arXiv 2510.16021 的 €19.9k/年 與 DK1 回收 38% / DK2 30%
   —— 差一個數量級就是自己程式錯了,當天就知道
2. **把不平衡價的「量」也建模** —— 現在 agent 只預測價差方向,沒用到系統缺不缺電的量
3. **重跑 `agent.py` 的敏感度**:訓練起點改成 2025-12-08(現在是 2025-10-01,含兩個月缺陷期)
4. 資料窗口往前推(`price15`/`forecast`/`calendar` 只到 2026-08-21,不平衡價已到 08-27)

### ✅ 上一輪(2026-08-28)做完的四件事

**① 不平衡價偏離跳三倍的問題,結案了 —— 那不是市場變劇烈,主要是定價規則換了。**
2025-03-18 起不平衡價開始吃 **aFRR 的啟動價**(2024 年沒有這個成份),而丹麥在 PICASSO 裡
**用很小的量清出很極端的價**(定價的格 aFRR 量中位 3.1–3.6 MW,三成不到 1 MW)。
DK2 分解:25.89(舊制)→ 43.00(只吃 mFRR 的反事實)→ **75.53(含 aFRR)**,
**aFRR 單獨佔總增幅的 66%**(DK1 63%)。
→ 🔴 **跨接縫比較 2024 vs 2025 量到的是市場改革,不是 agent 績效。目標函數只能在同一制度期內估。**

**制度日期從 2 個變成 4 個**(完整記錄在 `DATA.md` §12):
`2025-03-04` mFRR EAM + 結算轉 15 分 → `2025-03-18` **aFRR 進定價(本輪查出來的)**
→ `2025-09-30` 日前轉 15 分 → `2025-12-08` **mFRR EAM 定價缺陷永久修正**。
🔴 **前九個月被已知缺陷污染(靠人工更正擋著)→ 乾淨窗口 = 2025-12-08 起。**
🔴 **不可宣稱「日前轉 15 分讓不平衡成本下降」** —— 與定價修正完全混淆。

**② 完美預知上界進 repo 了**(`new_src/trading/oracle.py`)。乾淨窗口、10 MW:

| | 15 分版 | 逐時版 | 15 分多賺 |
| --- | --- | --- | --- |
| DK1 | 4,433 k€/年 | 3,972 k€/年 | **+11.6%** |
| DK2 | 5,100 k€/年 | 4,638 k€/年 | **+10.0%** |

舊的 scratch 初值(DK2 +9.5%)在同一個窗口重現了。
🔑 **固定單邊策略只拿到上界的 1.2–2.5% → 價值幾乎全在「押對邊」。**

**③④ 交易 agent 骨架 + 特徵 ablation** —— 🔴 **結果是一個乾淨的負面結果,而且它很有用。**

日前關門(D−1 12:00 CET)決定 D 日全部 96 格的部位,逐月重訓前推,測試 2026-03→08
(16,600 格)。**兩種特徵集都跑**(差別見下面的 `BID_TIME_SAFE`):

| 特徵集 | DK1 回收 % | DK2 回收 % |
| --- | --- | --- |
| **★ 投標時點確定拿得到(19 個)** | **0.6%** | **8.7%** |
| 加上 ENTSO-E 日前預測(26 個) | 3.8% [−2.7, 10.0] | 1.8% [−6.9, 10.9] |

🔴 **四個數字全部跟 0 分不開**(95% 按日 bootstrap CI)。前推預測與實際 dev 的相關 ≈ **+0.02**,
**R² ≈ 0.000**。

**ablation 排不出順序,而那本身就是結論。** 噪音底線(CI 寬度)是 12.7 / 17.8 pp,
而所有「少賺 pp」都在 ±9 以內 —— 抽掉某些組甚至讓回收**變高**。**最硬的證據就在上面那張表**:
多給 7 個特徵,**兩區往相反方向動**(DK1 +3.2pp、DK2 −6.9pp)。那不是特徵重要性,那是噪音。

### 🔴 `BID_TIME_SAFE`:ENTSO-E 的日前預測不保證趕得上關門

EU 543/2013 只要求日前負載預測與日前風光預測在 **D−1 18:00 CET 之前**公布 —— **比 12:00 的
關門晚六小時**。實務上多數 TSO 早上就發,但那是慣例不是保證,而且我們存的 parquet 只有
「對哪一格」沒有「何時發布」,**在地端驗不了**(要拉 ENTSO-E API 的 `createdDateTime`)。
→ `agent.py` 預設 `BID_TIME_SAFE = True`(19 個特徵)。
→ ⚠️ **這不影響階段 0 的結論**,只代表它偏保守:給了可能不該有的資訊,agent 還是學不動。
→ 📌 Energinet 自己的 `ForecastDayAhead` 風險低得多(它存在的目的就是服務中午那場拍賣)。

**負面結果的可信度靠三個檢查**(全在腳本的 self-check 裡):
- **leak canary**:挖掉關門後的價格,那天 26 個特徵一格不變
- **in-sample 作弊回收 93.8%**(相關 0.89)→ **管線是好的**,學不動的是市場
- **沒有白吃的午餐**:「永遠押同一邊」的平均 dev 兩區都不顯著(按日 bootstrap,CI 跨 0)

### 🔴 又試了七個變體,**還是沒有** —— 而且這次是預先註冊驗證的

`new_src/trading/agent_search.py`。**七個變體在跑之前就寫死**,分兩段時間:
選擇窗 2026-03→06 挑一個,**驗證窗 2026-06→08 只讓贏家跑一次**。

| 變體 | 針對什麼 |
| --- | --- |
| ① 基準 LightGBM 回歸 | — |
| ② Ridge 強正則化 | R²≈0 時樹可能只是在學噪音 |
| ③ **+歷史預測偏差** | 🔑 新資訊:日前預測若在某情境系統性偏高,那個偏差用**落後 2 天**的資料學得到 |
| ④ +偏差 · Ridge | ②③ 合併 |
| ⑤ 拆解(方向機率 × 歷史幅度) | dev 尾巴肥,平方誤差被極端值主導 |
| ⑥ 拉長訓練(2025-03-18 起) | 資料量 |
| ⑦ +偏差 · 不確定就不進場 | 動作空間加「空手」 |

**選擇窗最好的:DK1「③ +歷史預測偏差」+4.98%、DK2「① 基準」+16.16%。**
**→ 拿到驗證窗:DK1 +1.27% [−6.5, +9.0]、DK2 +0.61% [−9.8, +10.7]。🔴 兩個都掉回噪音。**

🔑 **這正是預先註冊要防的事**:DK2 那個 +16.16% 看起來很像成功(CI 只差一點就離開 0),
但它在完全沒碰過的三個月上只剩 +0.61%。**如果沒有事先切出驗證窗,這一輪會宣布 B 成功。**

⚠️ 一個還沒試的敏感度:訓練起點改成 **2025-12-08**(只用乾淨窗口)。沒放進這批是因為
變體清單在跑之前就定案了,**事後加變體會破壞預先註冊**。要試就當**新的一輪**、重切驗證窗。

### 🔴 第二輪:修好特徵接法 + 短 rolling window,**還是沒有**

起因是使用者的兩個質問,**兩個都成立**:

1. **「不平衡價順著出來就好,為什麼砍到落後 2 天」** → 對。原本一律落後 48 小時是實作偷懶,
   浪費 12–36 小時的合法資料。改成 **as-of 關門**(`agent.ASOF_FEATS`):窗口收在關門前 1 小時、
   day-ahead 價落後 1 天就用。⚠️ 實測差別 ≈ 0(dev 的 ACF:13h +0.008 / 24h +0.013 / 48h −0.004)。
2. **「A 能用 rolling window,B 為什麼不試」** → 對。第一輪只測**拉長**訓練,**沒測縮短**。
   第二輪補了最近 6 個月 / 3 個月。
   ⚪ 但 **detrend 轉移不過去**:A 的 detrend 治的是目標水準漂移,而 **dev 本身已經是個差、
   平均 ≈ 0、沒有趨勢**,沒有東西可以去掉。

**結果:六個變體,兩區的贏家在驗證窗都沒撐住。**

### 🔴🔴 這一輪最重要的產出:**我的顯著性門檻原本是壞的**

DK1 的贏家在驗證窗拿到 **+3.97%,CI [+0.7, +7.5] —— 通過了原本的門檻**。
**如果就這樣宣布,論文裡會有一個假結果。**

讓我起疑的是四個矛盾訊號(不是先跑檢查才發現的):
①它在**選擇窗是負的**(−1.72%),只是六個裡最不差 ②預測與目標**負相關**(−0.019)
③**DK2 完全沒有**(−4.26%) ④驗證窗已被用過一次。

查出來的原因:**按日 bootstrap 只量「抽哪些天」的不確定性,沒量「模型訓練本身」的。**
同一個變體只換 random seed,回收在 **−4.53% ~ +4.29%** 之間跳,標準差 2.74pp
—— **比那個 CI 整段還寬。**

> ✅ **從此門檻是兩條:①CI 離開 0 ②八個 seed 全部同號。缺一不算。**
> 已寫進 `agent_search.seed_spread()`,連同經過。

🔑 **這一輪一共攔下三個會產生假結果的東西,全部是被檢查攔的,不是被直覺攔的**:
as-of 特徵偷看關門後 15 分鐘(leak canary)· 選擇窗的贏家在驗證窗歸零(預先註冊 holdout)
· CI 通過但換 seed 就變號(seed 檢查)。

### 🔑 為什麼學不動 —— 訊號存在,但在關門之後才出現

`agent.py` 的 `horizon_probe()`(**故意 leak,只當診斷**)用再生能源**預測誤差**押一條規則
(發電比隔日預期少 → 押缺電):

| 誤差看到的時點 | 與 dev 的相關 | 照它押的回收 |
| --- | --- | --- |
| 交割前 5 小時 | −0.07 | **+12%** |
| 交割當下 | −0.10 ~ −0.12 | **+16~17%** |

**一條沒有訓練的規則,贏過全部日前特徵一個數量級。**
→ 🔑 **綁住階段 0 的是資訊集,不是模型。** 而且這跟發現①是同一件事的兩面:
2025-03-18 之後不平衡價由**交割前幾秒到幾分鐘、幾 MW 的 aFRR/mFRR 啟動**決定
—— 那個量在任何日前發布的資料裡都不存在。

→ **階段 0 沒有 RL 的位置這件事,現在是量出來的,不是假設的。**
→ **15 分鐘改制多出來的 +10~11.6%,要有日內資訊或實體彈性才拿得到** —— 那正是階段 1–3。

完整表格見 `figs/trading/AGENT_STAGE0.md`(重跑 `agent.py` 可重生)。

### ✅ 電價預測模型(模型 A)大幅改進,而且查出一個新的制度斷點

掃了 **44 個設定**(重訓頻率 × 逐小時建模 × 目標變換 × 模型類別),`new_src/models/experiments.py`。
**兩個價區的贏家是同一個**,而且它是唯一在斷點後不退化的:

**★ BEST = Ridge+Lasso+LightGBM 三者平均 · 只用最近 12 個月重訓 · 預測「相對昨天同一小時的變化」**

| | 基準(訓練一次) | ★ BEST | 改進 |
| --- | --- | --- | --- |
| DK1 | MAE 20.09 · rMAE 0.66 · R² 0.706 | **17.60 · 0.58 · 0.754** | **−12.4%** |
| DK2 | MAE 23.10 · rMAE 0.72 · R² 0.661 | **18.70 · 0.58 · 0.736** | **−19.0%** |

🔴 **這是去 leak 後的數字。** 先前報過 17.10 / 18.10,那含兩個關門後才知道的特徵
(`load_lag24_mwh` / `residual_lag24_mwh` = 昨天同一小時的**實測**負載與殘餘;
目標在中午後就是關門之後才量到的)。**那兩欄是 repo 原本就有的,不是這輪加的**,
所以舊基準也一樣虛高,改進幅度大致不變。已補進 `forecast.LEAK_COLS`,
並加上**合法替代**(`*_lag48` = D−2 同一小時,關門時一定已公布)。

配對按日 bootstrap 的 ΔMAE:DK1 [−2.50, −1.57]、DK2 [−4.40, −3.51],**都遠離 0**。
已接進 `forecast.BEST`,跑 `python new_src/models/baseline.py` 就看得到。

### 🔑 新發現:**2025-10 的日前 15 分鐘改制,在電價預測上留下可測的結構斷點**

`BATTERY_TRACK.md` 記的「LightGBM 最好、rMAE 0.54」**已經作廢**。資料延到 2026-08 後
LightGBM 掉到 rMAE 0.84。逐月追下去,斷點正好在 **2025-10**:之後電價水準上移
(DK1 月均 79 → 97),三個模型的偏誤分道揚鑣 ——

| 2025-10 之後的平均偏誤 | |
| --- | --- |
| Lasso 逐月重訓 | −2.0 |
| Lasso 訓練一次 | −6.3 |
| **LightGBM 逐月重訓** | **−23.3** ← 連重訓都救不了 |

🔑 **機制:樹的葉子值是歷史目標的平均,不能外推價格水準**;而擴張式訓練窗把新制度
稀釋在七年舊資料裡。線性模型靠燃料價係數就把整條預測抬高了。
⚠️ **但 LightGBM 在前半期其實是全場最強(DK1 MAE 16.67)** —— 不是「樹比較差」。
✅ 對症修法(最近 12 個月 + detrend)把它從 21.33 拉到 17.71。

🔴 **挑模型要看前後半期,不是只看總平均。** 舊冠軍(逐月+逐小時)DK1 前半 16.99 / 後半 18.07;
新冠軍 17.14 / 17.05。

### 📌 對照組(論文要拿來對答案的)

arXiv 2510.16021(DK1/DK2/SE3/SE4,2021–2024,太陽光電日內交易 RL):
10 MW 太陽電廠實得 **€19.9k/年**、**DK1 回收 oracle 的 38%、DK2 30%**。
→ 階段 1 一接上太陽就跟它對答案,**差一個數量級就是自己程式錯了**。

---

## 這一輪結束時的版控狀態(2026-08-27)

```
5649eb4  chore: drop the Cournot and multi-agent battery machinery
2ca1fe2  feat: pivot to the trading-agent track, and cut the docs to one entry point
a77604b  (tag: battery-track-final)  feat: joint dispatch LP, 2035 scenarios, hindcast
```

**三個 commit 與 tag 都已推上 origin。** 刪掉的 12 個檔全部在 tag 裡:
battery 五支 · `requirements.txt`(pin 版本)· `Dockerfile` · `docker-compose.yml` · `eda.ipynb` · pptx

```bash
git show battery-track-final:requirements.txt          # 看
git restore --source=battery-track-final new_src/battery/   # 拿回工作區
```

⚠️ **`requirements.txt` 已刪 → 現在沒有 pin 版本清單。** 要重建環境從 tag 取回。

---

## 該讀哪一份

| 文件                                         | 讀它幹嘛                                                    | 狀態            |
| -------------------------------------------- | ----------------------------------------------------------- | --------------- |
| **本頁**                                     | 入口                                                        | ✅              |
| [`DATA.md`](DATA.md)                         | **資料源、欄位、坑**。要碰資料一定先讀                      | ✅ 現行         |
| [`THESIS_DIRECTION.md`](THESIS_DIRECTION.md) | 熱側主線:研究問題、政策背景、基準數字、**§10 撤回過的說法** | ✅ 現行(熱線)   |
| [`MODEL_2035.md`](MODEL_2035.md)             | 熱側的 8-agent 聯合 LP 與 2035 情境,完整記錄                | ✅ 現行(熱線)   |
| [`STATUS.md`](STATUS.md)                     | ⚠️ **只剩 §4 參數清單 / §7 / §8 / §9**,其餘已移入 `archive/`     | 🟡 部分過時     |
| [`README.md`](README.md)                     | repo 說明、結構、怎麼跑、工作慣例、已知的坑(已瘦身到 112 行) | ✅ 現行         |
| [`BATTERY_TRACK.md`](BATTERY_TRACK.md)       | 🔑 **交易線的前情提要** —— λ 為什麼是 0、預測管道長什麼樣   | 🟡 已結案但有用 |
| `archive/LITERATURE_battery.md`             | 🔴 **整份是電池線的定義,待重寫**                            | 🔴 過時         |
| `archive/`                                   | 封存,不再維護                                               | ⚪              |

## 交易線的腳本(全部從專案根目錄跑,各自有 self-check)

```bash
python new_src/trading/imbalance_regimes.py   # 四個制度期 + 反推定價規則(約 5 秒)
python new_src/trading/oracle.py              # 階段 0 完美預知上界,兩種解析度(約 5 秒)
python new_src/trading/agent.py               # 日前 agent + 特徵 ablation(約 10 分鐘)
```

產出寫到 `figs/trading/`。**`agent.py` 的 self-check 是一個 leak canary**:
把日前關門之後的價格全部挖空,那一天的 19 個特徵必須一格不變。

---

## 🅿️ 明確不做(不要再提)

**λ / 內生價格 / 賽局均衡 · 市場競爭當論文主軸 · mFRR 與備轉市場 · 連續日內交易
(需要 XBID 訂單簿,付費拿不到) · 內生投資決策 · LOLE · 機組啟停 · 屋頂光電小戶
(需要先建模聚合商層)**

## 工作慣例

- **抓資料窗口一律 2019-01-01 → 今天**(`new_src/data/window.py`)
- **舊檔一定要退場**,`retire_superseded()` 先驗證再刪 —— 寧願炸也不要靜默拿到舊檔
- **raw 永不合併**:15 分鐘的存 15 分鐘,要逐時在分析層 `resample('1h').mean()`(價格是強度量)
- **天氣與發電特徵只准用 day-ahead 預測**,短前置欄位會 leak
- **不要編數字**;巧合不是證據;寧願被告知缺什麼資料
- Energinet 的 **429 是 IP 級冷卻**,退避要 30s–600s,而且**先抓再探索**
