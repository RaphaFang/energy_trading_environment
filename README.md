# 丹麥能源市場多 agent 模擬(碩論)

現行主線:**DK2(大哥本哈根)的 CHP + 區域供熱(fjernvarme)多 agent 模擬**。
電池線已於 2026-08-04 結案(park),結論與程式碼保留在 `BATTERY_TRACK.md`。

---

> 🧭 **2026-08-16:論文方向正在轉向。先讀 [`THESIS_DIRECTION.md`](THESIS_DIRECTION.md)** ——
> 從「市場力 / 策略行為」轉到「產能替代的三面向後果(電力適足性 / 供熱來源 / 供熱成本)」,
> 完全躲開 λ。下面這段交接狀態仍然有效(資料、模型、參數都沒變),但**⑦ 的下一步已被
> `THESIS_DIRECTION.md` §11 取代**。

> 🔄 **2026-08-18/19:使用者要求「完全重新思考路徑,從最初的設計一個分岔一個分岔重走」。**
> **這一輪還沒走完** —— 走到「B1 縮約式 λ」就停住,agent 靠什麼互動(F1)還沒開。
> 產出:`new_src/eda_price/`(價格形成診斷)+ `figs/price_formation/`(6 張圖)。
>
> **四件會改寫敘事的結論**:
>
> 1. 🔑 **「多 agent 彼此競爭推動價格」與「λ」是同一件事** —— 報高於成本只有在能推高價格時
>    才有利,那就是 λ>0。**不能靠多加 agent 繞過 λ。**
> 2. 🔑 **雙算是「公式」的病不是「資料」的病** —— 知道全部成本也救不了 `p = p_hist + λq`;
>    改成成本式出清就消失,即使成本資料很爛。**「不知道所有電廠成本」不該當論文理由。**
> 3. 🔑 **「價格外生」的理由要換掉** —— 不是「λ 估不出來所以躲開」(守勢),而是
>    **「丹麥是被互聯吸收的小開放價區,本地行為對價格的影響物理上趨近於零」**(攻勢)。
>    已量化(2026-08-20 直接比鄰國價格):**DK2 有 88.4% 的小時價格與至少一個鄰國完全相同**,
>    真隔離 11.6%。⚠️ 耦合率在 **2024-11 斷崖**(FBMC 上線),但那是**解析度假象**
>    (換 €1 門檻只掉 14pp、平均價差僅價格標準差的 1%,**別用「完全相等」當判準**),✅ 而**最吃緊的 10% 小時
>    耦合率反而略高(88.7%)** → price-taking 在論文最在意的小時站得住。
>    🔴 舊寫的「0.05–0.86%」是電纜滿載的間接代理,**低估 15–200 倍,作廢**。
> 4. **DK1 尖峰時 79.8% 的毛進口是路過的,而且是淨出口** → **「只做 DK2」從資料方便
>    升級成研究理由**;而且 DK1/DK2 分屬**不同同步電網**,這個邊界在電氣上是乾淨的。
>
> 🔴 **同時修掉五個資料問題**(15 分鐘制、`TotalLoad` 壞值門檻、滿載分母、
> `ExchangeContinent` 對 DK1 是德+荷合計、中位數相加≠逐時合計中位數),
> 並補上先前一直缺的**第三份資料:聯絡線的最大商業容量**(Energistyrelsen AF25 表 3,
> 已轉錄成 `new_src/eda_price/interconnectors.py`)。**全部細節在 `DATA.md` §9。**

# 🔴 交接狀態(下一個 session 先讀這段)

**最後更新 2026-08-15。**

## ⓪ 三十秒版本

|                     |                                                                                                                                                                          |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **研究範圍**        | **只做 DK2**。DK1 的程式碼與佔位值留著不刪,但別再往那邊投資                                                                                                              |
| **模型層級**        | 單一 agent、price-taker、perfect foresight 的排程 LP。**多 agent 層 0%** ← 現在唯一重要的缺口。agent 的定義已定案(⑥)                                                     |
| **排程行為**        | 🔴 **年佔比對得上,日內時點是反的**。LP 對價格系統性過度反應;已排除燃料價/稅費/最小負載,主因未定 —— 見 ②(h)                                                               |
| **技術參數**        | DEA Technology Catalogue 真值,基準已統一(見 ②)                                                                                                                           |
| **資料**            | 熱需求與電價 **2021-01 → 2026-07 全部完整**,約 48,800 小時、**5 個完整冬天**                                                                                             |
| **能跑的 DK2 機組** | 🟢 **4/6**(Amager、Avedøre、ARC、ARGO,約佔容量 91%)—— 2026-08-15 用**假定的生質價**解封兩台抽汽式。⚠️ **仍是四次獨立單機跑,不是多 agent 模擬**,見 ⑦ Phase 2              |
| **外部參數**        | ✅ **2026-08-15 `PLACEHOLDERS` 清空**:τ=1.07 / κ=8.50、**θ_h=26.84**(排放係數級距 2026-08-15 修正過,舊值 23.85 作廢)、θ_f 排除。見 ⑤b / ⑤c                               |
| **最大阻塞**        | ~~生質燃料價~~ 🟢 **資料已抓進 `new_data/fuel/`**(丹麥海關 + 瑞典能源署,2025 重疊年互相驗證到 ±7%)。⚠️ 木片兩條序列**壞的方式相反**,用區間掃描不要選一個 —— `DATA.md` §8 |
| **版控**            | 分支 `feat/heat-chp-track`,**19 個 commit 已推遠端**(工作樹乾淨);`main` 停在 `c1ce3aa`,**PR 未開**                                                                       |
| **下一步**          | 🔴 **2026-08-15 定調:停止完善熱側,把 agent 長出來。** ① 生質價接進模型(**是核心假說的開關,不是完善**)② 多 agent 層 ③ 寫作。見 ⑦                                          |

開 PR:https://github.com/RaphaFang/energy_trading_environment/pull/new/feat/heat-chp-track

## ① 這一輪(2026-08-09 ~ 15)做完的事

| commit    | 內容                                                                          |
| --------- | ----------------------------------------------------------------------------- |
| `ce7b6e4` | varmelast 端點性質查證:消費 vs 生產分開,修正 DK2 佔比                         |
| `027c216` | 三個效率基準不一致(`eta_el` ×3 + `cop_ref`)全修,加防迴歸 self-check           |
| `21c2d52` | `assumptions.py`、成本函數合併成一條、`dea.get()` 不再造假、`dk2_fleet.py`    |
| `3fabeed` | 15 分鐘電價接上、varmelast 5 個缺季補回                                       |
| `2c32010` | **背壓機組支援** → DK2 的垃圾焚化解鎖                                         |
| `2dd205f` | **`validate.py`:用分項產熱驗排程「時點」** → 抓到「年佔比對、時點反」,見 ②(h) |
| `2f2a037` | 最小負載(目錄真值)+ 39 小時假零熱需求;**證明最小負載只補 13%**                |
| `80134eb` | 分開「生質價擋住水準」與「不擋時點」;SØB25 生質價可讀(2025+)                  |
| `6b9bbf5` | **θ 掛錯變數** → 拆成 θ_f(掛 F)/ θ_h(掛 Q),見 ⑤b                              |
| `4f23e36` | **τ/κ 填真值、θ_h 用公布區間收掉** → `PLACEHOLDERS` 清空,見 ⑤b                |

**這一輪的方法論主線**:從「驗水準」轉向「**驗時點**」。三個尺度無關的診斷
(日固定效果 / 移除月×時形狀 / 日總量拆解)抓到了年佔比看不出來的東西,
而且連續推翻了**五個**我自己的推測 —— 都留在 ⑨ 與 `STATUS.md` §8–9。
(第五個是最後一天才抓到的:**「θ_h 門檻 73、餘裕 18%」是格點假象**,見 ⑤b。)

## ② 🔑 這一輪最重要的發現(花最多時間找出來的,不要重做)

### (a) varmelast 端點**同時含消費與生產**,混用 = 循環論證

`/api/v1/heatdata/historical` 回傳兩類欄位,官方定義快照存在
`new_data/heat/varmelast_dictionary.json`(**動態頁面,這是唯一的存證**):

| 欄位                                     | 官方 title           | 類別                 | 能不能當 LP 輸入            |
| ---------------------------------------- | -------------------- | -------------------- | --------------------------- |
| `BE-EO-CTR-EFF` / `DAP-VEKS-FORBRUG-EFF` | CTR / VEKS           | **消費**             | ✅ **只有這兩欄可以**       |
| `TOTAL`                                  | **Produktion i alt** | 生產                 | ❌ 已含蓄熱調度與經濟最佳化 |
| `BE-VL-*`(11 個分項)                     | 分來源生產           | 生產                 | ❌ 僅供驗證排程行為         |
| `BE-VL-TOTAL-FAK`                        | **CO2 - Udledning**  | **`Kg/GJ` 排放強度** | ❌ **不是熱量欄**           |
| `LOCAL`                                  | Lokal produktion     | 生產                 | 全期恆為 0,無資料           |

- 🔴 **`BE-VL-TOTAL-FAK` 誤當熱量欄**是舊記錄 64.4/27.3/4.5 的成因。**正確佔比(2023–25)**:
  熱電 **64.6%** / **垃圾焚化 27.7%** / 尖峰氣 4.0% / 工業餘熱 1.0% /
  **電鍋爐 0.82%** / **熱泵 0.61%** / 其餘 1.3%。
- **是事後實測不是事前計畫**:dictionary 寫「延遲 90 分鐘、每 5 分鐘更新」、CO2 說明寫
  熱電/焚化排放是 `målte værdier`(量測值)。
- **官方定義**(首頁):`Varmeplanen skal opfylde fjernvarmeselskabernes daglige prognose
for varmebehov` → **varmebehov = 熱網公司的需求預測;varmeplan = Varmelast 的調度**。
- **純需求預測在另一個 host**:`app-lasso-api-prod-001.azurewebsites.net/api/prognosis/overall`
  (「14-døgns varmeprognose」)—— 但**只往未來 14 天、只有日均、無歷史**。
- **生產 − 消費 = 蓄熱 + 損失**:dictionary 明寫 `Produktion i alt *eksklusive op- og
afladning på varmelagre`(**不含**蓄熱充放)。實測 gap 均 +21 MW、std 99、**41% 小時為負**、
  日內有充放循環;月均佔消費 **1.0–3.7%**(≈2%,**是等比例的不是固定 21 MW**)= 傳輸網損失。
- ✅ **HOFOR 有被計入**(查證過):官網「CTR og VEKS ejer hver et transmissionsnet」=
  只有兩個傳輸網,HOFOR 是配網商。CTR/VEKS 消費比 70.9/29.1 vs 官網戶數 340k/500k = 68/32,吻合。
- ⚠️ **CTR+VEKS 是「傳輸層取用量」不是終端消費總量**(有熱由本地直接進配網、不經傳輸網)。
  對本模型正好是對的邊界,但論文要標明口徑。

### (b) 三個效率基準不一致(全修了,別再改回去)

**目錄的 `Cb` 是用 name plate 效率算的** —— 拿**全部 16 張可比對的背壓表**驗證
(只有背壓表同時列電效率與熱效率):name plate 的 η_el/η_th 對 `Cb` 的誤差**全部 ≤0.0049**,
annual average **最小也有 0.0061**,兩群完全不重疊。`dea.demo()` 每次重跑這個推導。

| 原型           | 舊(annual,錯) | 現在(name plate)                 |
| -------------- | ------------- | -------------------------------- |
| `wood_chips`   | 0.409         | **0.430**                        |
| `wood_pellets` | 0.425         | **0.447**                        |
| `gas_cc`       | 0.56          | **0.59**                         |
| `coal`         | 0.485         | 0.485(該表本來就只有 name plate) |

🔴 **第三個同型 bug**:`cop_from_temp` 的 `cop_ref` 預設寫死 **3.2**,而 `Plant.cop_ref`
是目錄的 **2.8**,且所有呼叫端都用預設值 → **實跑 COP 全期高 14.3%、熱泵買電低估 12.5%**,
方向正好**高估 power-to-heat**(C3 章主題)。已改成 `None → Plant.cop_ref`,不再有第二個來源。

⚠️ **代價**:name plate 是設計點效率,LP 沒建停機與最小負載 → **系統性樂觀**。
可量化的補償 `dea.availability()`:木片 **0.914** / 氣 CC **0.927** / 煤 **0.950**
(多數表沒有 `Availability` 欄,用 `(1−forced)×(1−planned週/52)` 合成)。
**目前只是資料,`solve()` 沒有用它。**

### (c) `dea.get()` 不再偷填中點 —— 而且抓到一條大魚

目錄對某些參數不給 ctrl 中央估計**是有理由的**:那是業主的設計選擇不是技術屬性(容量最常見)。
舊版回 `(lower+upper)/2`,現在拋 `NoCentralEstimate`。

**氣 CC 只有 lower=100 / upper=500**,舊版填的 300 一直被當「目錄真值」引用:

```
gas_cc p_max=100  →  €  32.4 /MWh_th
gas_cc p_max=500  →  €  −5.1 /MWh_th     ← 正負號翻轉
      舊版 300     →  €   6.1 /MWh_th
```

**那個編出來的中點一直在扛結果。** 現在 `_real_demo` 兩端都跑。

### (d) 成本函數合併成一條(`chp._cost_coeffs`)

```
c = (p_fuel + ef·p_CO2 + θ)·F + vom_e·P⁺ + vom_th·Q − p_el·P + (τ+κ)·P⁻
```

燃料別靠**參數歸零**切換,零分支。P⁺/P⁻ **不需要 `max()`** —— 賣電與買電本來就是不同的
變數區塊,方向在建模時已知,所以 `max(−el,0)` 在建矩陣時就算掉了,LP 仍是線性的。
**有等價性 self-check**:vom=0、稅費=0 時逐項還原重構前那五行係數。

### (e) 背壓機組 = **一個界**,不是一套新矩陣

```
加一條  P − Cb·Q ≤ slack
slack = p_max  →  恆成立不綁(P≤p_max 且 Cb·Q≥0)= 抽汽式保有可行域面積
slack = 0      →  與既有的 P ≥ Cb·Q 夾成等式 P = Cb·Q = 退化成一條線
```

約束矩陣結構完全相同,**機組型式是參數不是分支**。
🔴 **背壓表的 `Cv=1.0` 必須歸零**(N/A 哨兵值)—— 照抄會讓容量線變成 `P + 1.0·Q ≤ P_max`,
憑空砍掉大半熱出力。`dea.plant_params` 已處理並有 assert 鎖住。

**順便解掉容量 NOT_FOUND**:`P = Cb·Q` ⇒ **`P_max = Cb · Q_max`** —— 背壓式的電容量是
熱容量的**後果不是獨立自由度**(`dk2_fleet.derived_mw_e()`,只對背壓式成立)。

### (f) DK2 真實車隊(`new_src/heat/dk2_fleet.py`)

| 機組                      | MW_th | MW_e           | 型式      | 業主            | 狀態                   |
| ------------------------- | ----- | -------------- | --------- | --------------- | ---------------------- |
| Amagerværket (AMV1+AMV4)  | 810   | **166**        | 抽汽      | HOFOR           | 卡生質價               |
| Avedøreværket (AVV1+AVV2) | 880   | **815**        | 抽汽      | Ørsted          | 卡生質價               |
| Køge Kraftvarmeværk       | 54    | NOT_FOUND      | NOT_FOUND | VEKS            | 卡生質價 + 型式未確認  |
| **ARC (Amager Bakke)**    | 250   | **70(反推)**   | 背壓      | ARC             | ✅ **可跑**            |
| Vestforbrænding           | 143   | NOT_FOUND      | 背壓      | Vestforbrænding | 卡「進傳輸網比例未知」 |
| **ARGO (Roskilde)**       | 120   | **33.6(反推)** | 背壓      | ARGO            | ✅ **可跑**            |

- **MW_th 六台全有**(varmelast 自己的說明文字,對熱容量是最高權威)。
- **MW_e 只有兩台查得到**。四台 NOT_FOUND **不是懶得查**:ENTSO-E 不收 <100MW、
  Energinet 只給**全市合計(是上界不是機組容量)**、業者官網是 JS 頁面。
  下一步要真值:能源署 **Energiproducenttællingen (EPT)**,或直接問 varmelast。
- **商轉年份只有 Amager 推得出來**(Energinet 逐月序列):**2019-10 出現第二台 ≥100MW
  = BIO4 併網;2020-03 掉回一台 = AMV3 燃煤 250 MW_e 除役**。
- **蓄熱槽三座**:Amager 1,000 MWh/±300 MW、Avedøre 2,200 MWh/±330 MW、
  Høje Taastrup 池儲 3,300 MWh/±30 MW(**週儲,年僅 25–30 循環**)。合計 **6,500 MWh**,
  而 `Plant.s_max` 只有 2,880(DEA 單一 TTES)→ **尚未對齊**。
- ⚠️ varmelast 自相矛盾:分項加總 熱電 1744 vs 自己公布 1600(+9%)、垃圾 513 vs 425(+21%)。

**ARC / ARGO 已能用真實 DK2 熱需求跑完整年:單位供熱成本 ≈ −€50/MWh_th**
(手算 gate fee/Cb/vom 對照 −€46)。**負值是對的** —— 垃圾廠收處理費,供熱是賺錢的,
這就是它們在 DK2 屬於 **prioriteret produktion**(優先生產)的經濟原因。

### (g) 資料窗口打開了

- **15 分鐘電價 `DayAheadPrices` 與逐時 `Elspotprices` 完美對接**:逐時最後一筆
  2025-09-30 21:00 UTC(涵蓋到 22:00),15 分從 **22:00 UTC** 開始 → **間隙 0 分鐘、無重疊**。
  DK1/DK2 各 29,184 筆到 2026-07-31,100% 非空,**相鄰間隔全部剛好 15 分**。
  ⚠️ 價格是**強度量**,聚合成逐時要 **mean 不是 sum**。存成單獨檔 `price15_*.parquet`。
- **varmelast 5 個缺季是抓取失敗不是沒資料**,`fill_gaps()` 補回 **10,944 列**
  (37,969 → **48,887**)→ **2021–2026 每年都完整,2022 能源危機年可用**。

**→ 熱側與電價的交集 = 2021-01 → 2026-07,約 48,800 小時、5 個完整冬天。**

### (h) 🔴 排程行為驗證:**年佔比對得上,日內時點是反的**(2026-08-12/13,`heat/validate.py`)

第一次拿 varmelast 的**分項產熱**驗 `chp.py` 的**時點**而不是水準
(`BE-VL-KRAFTV/AFFALD/EVO/VP/SPIDS-GAS` 幾乎一對一對應 LP 的 `Qc/Qe/Qh/Qpb`)。

**同一段 2024 真實 DK2 資料,LP(真實氣價 + κ=25.3)vs 實測**:

| 指標                | 實測       | LP         |                   |
| ------------------- | ---------- | ---------- | ----------------- |
| 尖峰鍋爐佔熱        | 5.21%      | **5.95%**  | ← 對得還行(1.14×) |
| 尖峰鍋爐**日內 ρ**  | **+0.164** | **−0.500** | ← **符號相反**    |
| 電鍋爐 日總 ρ(價)   | −0.256     | **−0.785** | ← 過度反應 3×     |
| 電鍋爐 日總 ρ(需求) | **+0.421** | **−0.099** | ← 實測靠需求決定  |
| 熱電 Qc 的 ΔR²      | 0.005      | **0.242**  | ← **48×**         |

🔴 **只看年佔比會誤判成「模型驗證通過」** —— 這是「驗證要挑有識別力的標的」的第二個實例。

⚠️ **2026-08-13 修掉兩個疊在一起的分母錯誤**(先前寫 5.12%,**那個數字不要再引用**):
LP 的分母是熱需求(CTR+VEKS 消費),實測卻用了生產分項加總(多 2.44% 的蓄熱+網損);
而且 LP 的 `Qpb` 對應實測的**氣+油**,不是上面 (a) 那個**尖峰氣單獨的 4.0%**。

✅ **時點結論對燃料價免疫**(2026-08-13 補測):不能用「垃圾費 vs 氣價」當兩端 ——
低端 CHP 太便宜,尖峰鍋爐全年只有 6 天有出力、電鍋爐 2 天,ρ 根本估不出來。
改成掃 CHP 燃料價:**尖峰鍋爐佔比 <50%(還是尖峰機)的整個區間內,日內 ρ 一律為負**
(−0.265 ~ −0.562);ρ 只在燃料價 80、佔比 93.9% 時才轉正,那時它已經是基載不是尖峰機。
📌 錨點:**SØB25 木片 2025 = €36.3/MWh_fuel**,落在穩定區間中央
→ **生質價擋住的是水準,不是時點。**

⚠️ **後續**:照這個結論去加**最小負載**(`solve(committed=True)`,目錄真值)了,
但**只補上尖峰鍋爐缺口的 1/8**(−0.52 → −0.43),對 P2H 的過度反應**毫無作用** ——
見 `STATUS.md` §8.5。**「缺最小負載是主因」這個推測只對了一小部分,不要再重複它。**
🔑 而且這個否定比看起來更強:線性下界沒有 0/1 開關 = 機組**完全不能停機**,
比真實 min-load 更嚴格;這麼強的約束下都只補 13% → **a fortiori 成立**。
順帶確認**爬坡率在逐時解析度下不綁**(每小時 2.4–9.0 倍滿載)→ 別去做爬坡約束。

🔴 **兩個 confound 還沒排除**(見 `STATUS.md` §8.6):
① 尖峰鍋爐是**無上限懲罰項**不是有容量的機組 —— 無上限又無啟停成本的鍋爐在 LP 裡是
**純價差套利工具**,與需求脫鉤,而真實尖峰機是容量驅動的。給它真實容量(1,300 MW_th)
測過,ρ 只從 −0.500 動到 −0.506、上限僅 1.2% 小時綁到 → **檢定太弱,排除不了**。
真正該測的是啟動成本,但**目錄的 `44 Natural Gas DH Only` 沒有 Startup cost 欄**。
② 用背壓垃圾原型代表 64.6% 是生質抽汽的車隊。

**三個診斷各擋掉一種假象**(細節見 `STATUS.md` §8.1):日固定效果擋季節性、
移除「月×時」形狀擋兩條固定曲線對齊、日總量拆「開多少 vs 何時開」。
⚠️ 上一輪那個「垃圾與熱電高價時出力更高」的直覺**在同日內是反的** —— 季節性陷阱又出現一次。

## ③ 現在擋路的東西(依重要性)

**1. 🟢 生質燃料價 —— 2026-08-15 從「最大阻塞」降級為「待使用者選型」。**
✅ **2025–2026 有 SØB25**(`assumptions.biomass_fuel_price_eur_mwh()`,木片 **€36.3**/MWh_fuel)。
🟢 **2021–2024 也有了,而且 2026-08-15 已經抓進 `new_data/fuel/`**(`new_src/data/biomass_prices.py`,
raw 儲存、有 skip-if-exists、self-check 重算三方交叉驗證)—— 兩個官方免費序列,**2025 重疊年互相驗證到 ±7%**:
瑞典能源署 PxWeb(逐季 1993+,värmeverk 的平均採購價)與 Danmarks Statistik `KN8Y`
(逐年 1988+,KN 8 位碼的金額 ÷ 重量 = 進口單價)。**完整比較表與四個使用前提在 `DATA.md` §8。**
🔑 **它順帶量化了一件事**:2021 的木片是 **€18.9–23.3**,2025 是 **€34.0–38.8**
→ **拿 2025 鋪滿 2021 會高估生質成本 55–90%**,方向是把生質機組推出 merit order,
而 2021–2022 正是最想研究的兩年。
⚠️ **仍然擋住的是水準,不是時點**(2026-08-13 掃描證實)。解掉後 `dk2_fleet.runnable()` 從 2/6 變 5/6。
⚠️ **使用者的決策未變**:採不採用、用哪一條、怎麼辯護跨國代理,是他的研究設計選擇。

**2. Vestforbrænding 進傳輸網的比例未知。** 它先供自己五個市的配網,**餘量**才給 VEKS/CTR
→ 143 MW_th 不能整台當 agent。

**3. 機組容量與熱網規模的對齊仍未全解。** 背壓式那三台解掉了(容量可反推),
抽汽式那三台仍是「單一機組典型值 vs 熱網規模」的老問題。

**4. 多 agent 層仍然是 0%。** 核心假說(多業者相關熱需求侵蝕稀缺時的彈性)
**目前無法用現有程式碼檢驗**。

## ④ 三件「使用者要自己處理、不要幫他猜」的事

| 事項                      | 狀態              | 為什麼不能猜                                                                                                                                      |
| ------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| **生質燃料價**            | 🟢 **來源找到了** | 原本的理由是「無可信來源」—— **2026-08-15 起不成立**(瑞典能源署逐季 + DST KN8Y,`DATA.md` §8)。**仍歸使用者決定**,但現在是研究設計問題不是資料問題 |
| **elafgift / nettarif**   | 🟡 部分解決       | 見 ⑤;`KAPPA_NET` 已有真值可用但適用邊界未定                                                                                                       |
| **DK2 熱網供水/回水溫度** | 🔴 仍缺           | `cop_from_temp` 裡寫死 70°C 是編的,直接決定熱泵 COP。CTR/VEKS 年報應該有                                                                          |

## ⑤ P2H 之謎:**2026-08-12 大幅收斂,但沒有完全解掉**

原本的謎題是「模型 8.67% vs 實測 1.4%」。用**真實 DK2 熱需求**(而不是 DK1 度日代理 ×8%)
與真實 DK2 電價重跑後:

```
                       κ=0      κ=25.3 EUR/MWh_e     實測 DK2 2024
P2H 供熱佔比           4.13%  →  2.43%                1.30%
尖峰鍋爐佔熱           5.78%  →  5.95%                5.12%   ← 這個一直都對
```

**差距從 6× 縮到 1.9×。** 三件事已經排除,兩件事還在:

| 候選解釋              | 狀態                                                                                                                                                                                                                                    |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~裝置容量太小~~      | ❌ **排除**。實測出力上界 = 容量下界:電鍋爐 **≥98**、熱泵 **≥25.9** MW_th —— 反而是 `chp.Plant` 的 DEA 單台值 20/10 **低了 4.9×/2.6×**                                                                                                  |
| ~~業者不看電價~~      | ❌ **排除**。電鍋爐是**全場對電價最敏感的熱源**(ΔR²=0.030 全場最高、日內 ρ=−0.261)                                                                                                                                                      |
| ~~用行為反推稅費~~    | ❌ **反推失敗**,見下                                                                                                                                                                                                                    |
| 🔴 負電價與熱需求錯位 | **部分成立**:負電價 888 小時中只有 **1.8%** 落在 11–2 月,那些小時熱需求僅全期均的 **55%**;熱需求最高十分位只有 0.33% 的小時電價<20(最低十分位 8.08%,**差 25×**)。⚠️ 但**不是「沒空間」**:負電價時非必發空間仍有 239 MW_th,實際只用 24.3 |
| 🔴 LP 對價格過度反應  | **成立**,見 ②(h)。這是模型端的問題不是現實端的                                                                                                                                                                                          |

🔴 **用顯示性偏好反推 τ+κ 失敗了,要記住這件事**:拿「電鍋爐開/關」去反推它實際付多少
稅費,估計值在不同樣本間**從 +€25.5 翻到 −€16.5**,而且準確率只比 κ=0 高 1–2 個百分點。
更關鍵的是 surplus 極不利時開機率仍有 20–29%、極有利時也只到 67–71%
→ **純邊際成本門檻規則解釋不了電鍋爐的開關**。
⚠️ 那個 +€25.5 剛好貼近 soeB25 的 €25.3,**不要拿它當證據** —— 它是較弱設定下的巧合。
**稅費的適用邊界仍然只能靠法規真值,不能靠行為反推。**

🚩 **elafgift 的資格條件可能才是關鍵**:要拿減免必須是「熱電機組能涵蓋一年至少 75%
總供熱量」的生產者 → **獨立的電鍋爐/熱泵不適用**。這解釋的是「為什麼沒人蓋」不是「為什麼不用」。

## ⑤b ✅ 四個外部參數全部結案(2026-08-14 拆分 → 08-15 填真值)

```
c = (p_fuel + ef·(p_CO2 + θ_f))·F  +  θ_h·Q  +  vom_e·P⁺ + vom_th·Q − p_el·P + (τ+κ)·P⁻
                        ↑ 天然氣            ↑ 垃圾焚化                        ↑ 只有買電才付
```

**先修位置(08-14)**:丹麥垃圾三稅(affaldsvarmeafgift / tillægsafgift / CO2-afgift)
**計徵基礎全部是熱**,而舊模型讓它們隨燃料走。**位置錯了掃描救不回來** ——
誤差方向取決於 `Q/F`(模型的內生輸出),而且會**偽裝成「模型對價格過度反應」**。
⚠️ 背壓關係 `Q = η_th·F` 一直在掩護這個錯誤。法源見 `STATUS.md` §9。

**再填真值(08-15)**:

| 符號         | 值                     | 依據                                                            | 狀態         |
| ------------ | ---------------------- | --------------------------------------------------------------- | ------------ |
| τ elafgift   | **1.07** EUR/MWh_e     | elpatronordningen **封頂退稅上限** 8.0 DKK/MWh_e,2021–2026 常數 | ✅ 結案      |
| κ nettarif   | **8.50** EUR/MWh_e     | Energinet `(6.1−0.5)+7.4×0.10 = 6.34 øre/kWh`                   | ✅ 結案      |
| θ_h 垃圾熱稅 | **26.84**(壓力測到 34) | 2026 公布值 **55.6** DKK/GJ_heat(配額機組級距);上界用 2024 代理 | ✅ 收掉,見下 |
| θ_f 氣碳稅   | **0**(維持)            | 掃描排除                                                        | ✅ 關閉      |

🔴 **θ_h 在 2026-08-15 下午改過一次,舊數字全部作廢**:排放係數從 0.02834(非配額級距,
**套錯**)改成 **0.0370**(配額機組法定備用值)→ θ_h 從 **23.85 → 26.84** EUR/MWh_th
(49.4 → **55.6** DKK/GJ)、ARC 供熱成本從 −23.70 → **−20.71**。理由見 ⑤c。
⚠️ 桌面那份 `TAX_PARAMS_FINAL.md` 寫的 23.9 是**舊值**。**引用寫 26.84 或 55.6 DKK/GJ_heat。**

**θ_h 收掉的理由**:採用值 **55.6 DKK/GJ_heat** 低於「垃圾機組不再全開」的穿越點,
而且垃圾在整個合理區間都是 **97.6–99.8% must-run** → **不必再去挖 2021–2024 的真值**。

🔴🔴 **但 2026-08-15 補跑 34 那一格時發現「門檻 73」是格點假象,舊寫法要作廢**:
demo 報的門檻 = **格點裡第一個非全開 ≥5% 的 θ_h**。舊格點 30 與 35 之間是空的 → 報 73;
把 34 加進去,同一支程式報 **70**。真正的穿越點在 **62–70 DKK/GJ 之間**。
⚠️ 連帶垮掉的是餘裕的說法:**代理上界 62 就貼在穿越點下緣,不是低於它 18%。**
✅ 結論仍成立,但理由換成:①採用的 55.6 距穿越點下緣 62 還有約 10% ②must-run 該用
**供熱佔比**判,而它到 34 都還有 97.6–98.7%。→ **論文寫區間,不要寫「遠低於門檻 73」。**

🔴 **結論寫法**:**不能寫「兩端無差異」** —— 非全開小時 0.5% → 3.0% 是 6 倍、到 34 是 18 倍,
口試會被咬。要把宣稱下移一層:「**供熱佔比全區間 97.6–99.8%,實質 must-run
→『垃圾焚化無策略調度邊際』這個結論對 θ_h 真值不敏感**」(宣稱研究結論一樣,不是宣稱數字一樣)。

🔴 **三件必須記住的事**:
① **θ_h 與 κ 是耦合的** —— 擠掉垃圾的是 **power-to-heat 不是尖峰鍋爐**,
所以穿越點由 κ 決定(κ=0 時往下移)。**不能各自判斷。**
⚠️ 兩邊都要用**同一組格點**才能比,否則比到的是格點差異(這次就踩到了)。
② **θ_h 從 0 變 26.84 不是零變更**:ARC/ARGO 供熱成本從 −47.5 移到 **−20.71**,舊基準作廢。
③ **κ 不可能解釋 P2H 的時點錯誤** —— nettarif 是**能源費、不隨小時變動**,只改水準。
→ 調頻收入那條線更孤立。

✅ **「門檻 73 是修 bug 前還是後跑的?」已查證:不受影響。**
`theta_h_threshold()` 根本不走 `run_model()` —— 它逐台跑真的垃圾廠(ARC/ARGO)、
θ_h 逐格明確傳入,車隊代理的錯配污染不到它。**73 可以進論文。**(詳見 `STATUS.md` §9.6)

🔴 **為什麼不用 SØB25 Tabel 10 的 €25.2 當 κ**(論文要交代):①SØB §4.1 明文那是
**社會經濟價格、不得用於企業經濟計算**;②該表含 avance(零售加成)與配電費,
而 TSO 接入的電鍋爐兩者都不付 → 真值只有它的約 1/3。
📌 先前行為反推得到 +€25.5 看似與之吻合 —— **那是巧合,而且吻合本身該視為警訊**。

🔑 **副產品:窗口起點的第四個獨立理由**。elpatronordningen 上限 2020 → 2021 砍 96%
(221.0 → 8.0 DKK/MWh_e,電鍋爐每 MWh_e 差 **EUR 28.6**)→ **2019–2020 的 P2H 與 2021
之後不是同一個制度期**。有人要把窗口往前拉時拿這條出來。

⚠️ **佔位符清空 ≠ 模型沒有假設**:θ_h 的 2021–2024 沒有公布值(用區間)、
`ef_chp` 對垃圾仍是量級值、`cop_from_temp` 的 70°C 供水溫仍是編的。
⚠️ **對稱性缺口**:CHP 側的 indfødningstarif + balancetarif(約 €1.5/MWh_e)該打在 `P⁺` 上,
**目前式子裡沒有**。補的時候有兩個坑:①**indfødningstarif 不是人人都付** ——
Energinet 明講太陽能、風機、以及仍受收購義務(aftagepligt)涵蓋的分散式電廠不付,
ARC/ARGO 是否在內要確認,不能一律套用;②**balancetarif 不是常數**(2024 = 0.24、
2025 = 0.65 øre/kWh,一年漲 171%),要進模型得做逐年序列。

## ⑤c 稅費定案之後的挑戰(2026-08-15;第 1 項當天下午已查證結案)

**1. ✅ 排放係數已結案 —— 但答案是「我們原本用錯級距」,而且餘裕被吃掉一半。**

起因是我們自己的文件對同一個係數給了兩個差 2.5 倍的值(§9.2 的 Skat 範例 0.070
vs 實作的 0.02834)。查 Skat 法律指引 E.A.4.5.7.3 之後,**兩個都不該用**:

| 情況                             | 該用的係數              | θ_h(2026)                   |
| -------------------------------- | ----------------------- | --------------------------- |
| **非**配額涵蓋機組               | 28.34 kg/GJ             | 49.4 DKK/GJ = 23.85 EUR     |
| **配額涵蓋**、知道自身實測係數   | **自身實測值**          | 未知                        |
| **配額涵蓋**、不知道自身實測係數 | **37.0 kg/GJ** ← 本模型 | **55.6 DKK/GJ = 26.84 EUR** |

- 🔴 **ARC/ARGO 熱輸入遠超 20 MW → 2013 起就是配額涵蓋機組**(Lov nr. 1353 af 21/12/2012)
  → **28.34 那列根本不適用**。已改成 0.0370,θ_h 23.85 → **26.84**
- 🔴 **0.070 不是稅率** —— 查證後它是 Skat 範例裡某假想配額廠的**自身實測係數**
  (`verificeret emissionsfaktor 70 kg CO2/GJ`),被抄進 §9.2 時誤植成「法定標準」
- 📌 **稅法級距都低於物理值**:能源署的垃圾總排放係數 101.7 ton CO2/TJ ×
  非生物份額 41.79% = **42.50**(這就是 `ef_affald=42.5` 的出處,排放清冊用的)
- ✅ **結論仍成立**(55.6 < 穿越點下緣 62)**但餘裕從 20% 縮到約 10%**
- 🔴 **殘留風險**:真正該用的是 ARC/ARGO **自身實測係數**,我們沒有。粗估約 35 kg/GJ
  (用 ARC 公布的煙囪年排反推,**不可引用**);若接近範例那種 70,**結論會翻掉**
- 🔴 **還沒做完的一格**:`WASTE_TAX_PROXY_DKK_GJ` 那三個代理值還是用舊的 28.34 建的,
  用 37.0 重建後 2024 那格約 **63.3**,**會跨進穿越點區間** → 見 `STATUS.md` §9.2b 末段

**2. 🔴 車隊代理的參數錯配不只 θ_h 一個。**
θ_h 被抓到只是因為它剛好從 0 變非 0。`run_model()` 那個代理裡 **64.6% 其實是生質**,
所以 `p_fuel`(垃圾負 / 生質正,**比 θ_h 大一個數量級**)、`ef`(42.5 / 0)、
`Cv`(0 背壓 / 0.14–0.17 抽汽)**從一開始就錯,只是沒有那個暴露時刻**。
→ **`run_model()` 產出的任何車隊層級數字都不該直接拿去跟實測比對**(見 `STATUS.md` §9.4c)。

**3. 🟡 2026 起 κ 有結構斷點。** Energinet 預計自 2026 年起把 TSO 接入用戶的 nettarif
改以**容量費(DKK/MW)**收取 → nettarif 從邊際成本整個消失,只剩 systemtarif 0.74 øre/kWh,
**κ 掉到 €1/MWh 量級**。窗口涵蓋 2026(目前資料到 2026-07)→ **論文必須註明**。

## ⑥ ✅ agent 的定義:**2026-08-15 使用者定案**

先前列為「待定案、別自己定」的兩件事,使用者已經拍板。**不要再重開這兩個問題。**

### ⑥.1 一個 agent 是什麼

> 使用者原話:「模型要做的是我主要賣電但同時有熱水場,你要最大化收益兩者,
> 這就是一個 agent 應該做的。」

|                | 定案                                                                     |
| -------------- | ------------------------------------------------------------------------ |
| **agent = 誰** | **廠級業主**(HOFOR、Ørsted、ARC、Vestforbrænding、ARGO)                  |
| **它在做什麼** | 手上有一座 CHP,**電賣到電力市場、熱送進熱網**,目標是**兩邊收益合計最大** |
| **目標函數**   | **利潤最大化(電+熱合計)**,不是成本最小化                                 |

⚠️ **這取代了先前討論裡「市政成本最小化、商業利潤最大化」的混合版本** ——
使用者用上面那句話把它統一成全部利潤最大化。
📌 **對現在跑得動的兩台其實沒差**:ARC/ARGO 燃料成本是負的,不管叫成本最小還是
利潤最大都是全開(已量到 97.6–99.8%)。**這個假設要到生質那三台解鎖後才會真的影響結果。**

### ⑥.2 🔑 但熱不是 agent 自己說了算 —— **兩層結構**

**DK2 的熱是 Varmelast 統一調度的**(見 ②(a):`Varmeplanen skal opfylde
fjernvarmeselskabernes daglige prognose for varmebehov`)。所以一個 agent 的決策是兩層:

```
第一層  它拿到多少熱的量  ← 由 Varmelast 按成本排序決定
                            agent 能影響的是**自己的成本有多低**,不是自己的量
第二層  拿到熱之後電出多少 ← 被機組型式綁住(背壓幾乎沒得選、抽汽有餘裕)
                            **剩下的那點自由度 = 這個 agent 的「彈性」**
```

🔑 **核心假說活在第二層**:熱需求高的時候,每一家的那點自由度**同時**被壓縮,
而那正好是系統最需要彈性的時候。

✅ **好消息:這個結構不需要內生價格或賽局均衡** —— 就是「N 個業者各自解自己的問題

- 一個中央熱量分配」。**電池線就是死在內生價格上的**(雙算無解,見 `BATTERY_TRACK.md`),
  這條路避開了它。

### ⑥.3 還沒定案的(只剩這個)

- **回測窗口**:資料上 2021-01 → 2026-07(5 個完整冬天),但 2021–2024 需要生質價。
  ⚠️ **不要拿 2025 的價格鋪滿所有年份** —— 已量化:2021 木片是 €18.9–23.3、
  2025 是 €34.0–38.8,**鋪滿會高估 55–90%**(見 `DATA.md` §8)。

---

## ⑦ 下一步:**停止完善熱側,把 agent 長出來**(2026-08-15 使用者定調)

> 使用者原話:「我這熱水的建立管道也不是最終結果,目標是建立 agent,
> 但現在 agent 都還沒長出來,這應該才是我要重視的,不要再一直完善熱水了。」

### ✅ Phase 1 — **2026-08-15 完成**(生質價用假定值,四台機組活過來)

**這不是「把熱側做得更完整」,它是核心假說能不能被檢驗的開關。** 理由:

「彈性」在 CHP 裡的定義是**「熱送出去的量固定時,電還能不能調整」**,
而這**完全取決於機組型式**:

| 型式     | 機組                       | 電熱關係                    | 有沒有彈性                   |
| -------- | -------------------------- | --------------------------- | ---------------------------- |
| **背壓** | ARC、ARGO、Vestforbrænding | `P = Cb·Q`,電是熱的固定倍數 | **完全沒有**。熱定了電就定了 |
| **抽汽** | Amager、Avedøre、Køge      | 電熱可互換,有一個可行區間   | **有** —— 彈性只在這裡       |

🔴 **現在跑得動的 ARC 與 ARGO 兩台都是背壓** → 今天蓋多 agent 層,會得到
**兩個幾乎相同、而且完全沒有彈性的 agent**,「彈性同時消失」這個假說
**在數學上就不可能被檢驗**。

✅ **卡住的那三台剛好全是抽汽式** —— DK2 的彈性幾乎 **100% 在它們身上**,
而且燃料成本為正(才會對電價有反應)、業主不同(才有「多個業者」可言)、佔供熱 **64.6%**。

**做了什麼**(使用者授權「先設一個假定值,不要再花時間算」):

- `assumptions.BIOMASS_ASSUMED_EUR_MWH` —— 2021–2025 逐年,**存區間的兩端**
  (丹麥海關 / 瑞典熱廠),數字是從 `new_data/fuel/` 的原始資料算出來的**不是編的**
- `assumptions.biomass_fuel_price_assumed()` —— **刻意跟原本那支分開命名**。
  原本 `biomass_fuel_price_eur_mwh()` 對 <2025 拋 `KeyError` 的行為**完全不動**
  (它擋的是「有人偷偷用單一值回填」)→ **不會有人不小心拿到假定值**
- `assumptions.ASSUMED` —— 新的一類警告(**有值但值是假定的**,與 `PLACEHOLDERS` 的
  「值是 0」性質不同),每次跑都印
- `chp.dk2_plant()` **擴充到抽汽式**:背壓用 `derived_mw_e()` 反推、
  **抽汽用 `dk2_fleet` 查到的 MW_e 真值**(抽汽有凝汽尾,電容量是獨立自由度,不能反推)
- `dk2_fleet` 解封 Amager / Avedøre,self-check 的鎖同步更新

**實測結果**(2024 年真實 DK2 資料,8,744 小時,各自單機跑):

| 機組    | 型式     | MW_th | MW_e | 燃料價 | 供熱成本 EUR/MWh_th |
| ------- | -------- | ----- | ---- | ------ | ------------------- |
| Amager  | **抽汽** | 810   | 166  | 40.20  | **+25.97**          |
| Avedøre | **抽汽** | 880   | 815  | 40.20  | **+0.55**           |
| ARC     | 背壓     | 250   | 70   | −26.19 | −29.77              |
| ARGO    | 背壓     | 120   | 34   | −26.19 | −32.09              |

📌 Avedøre 幾乎打平是合理的:815 MW_e 對 880 MW_th = 電力主導,賣電收入幾乎抵掉燃料。
⚠️ **ARC 這裡的 −29.77 跟 `STATUS.md` §9.4 的 −20.71 不是同一個設定**
(那邊關掉蓄熱槽、需求縮放方式不同)。**引用前先確認是哪一組。**

🔴 **仍不可跑的兩台,卡的都不是燃料價**:Køge(機組型式與電容量未確認,佔容量僅 2%)、
Vestforbrænding(進傳輸網的比例未知)。**都不值得為了它們卡住 Phase 2。**

### 🔴 Phase 2 — 多 agent 層(**論文的核心**)

1. 把 DK2 熱需求依 Varmelast 的成本排序**分配到各 agent**(⑥.2 的第一層)
2. 每個 agent 用現有 LP 解自己的利潤最大化(⑥.1)
3. 聚合成**彈性帶寬**:每小時「在滿足已分配熱量的前提下,電還能往上/往下調多少」
   → 這是 `flexibility.py` 已在做的事的多 agent 版,**可以複用**
4. **檢驗核心假說**:彈性帶寬 vs 系統稀缺指標(殘餘負載、價格尖峰)的相關

### Phase 3 — 寫作

`LITERATURE.md`(範圍已重新界定:調度模型驗證 + 多 agent 彈性)→ 論文章節。

### 🅿️ 明確 park 掉、不要再碰

| 項目                                      | 為什麼                                                                                                                              |
| ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| **mFRR / 備轉市場**                       | 🔴 **2026-08-15 使用者明確叫停**:「我東西已經夠複雜了」。時點問題保持「已知的失效模式」,論文當**驗證框架的發現**寫,不是當待修的 bug |
| 內生價格 / 賽局均衡                       | 電池線死在這裡(雙算無解)                                                                                                            |
| 時點問題的根因                            | 同上,已轉為論文的一個發現                                                                                                           |
| 熱網供水溫度、蓄熱槽對齊、`plant.py` 重構 | 都不擋 Phase 2                                                                                                                      |
| θ_h 代理值用新級距重建                    | 尾巴,不擋(見 `STATUS.md` §9.2b 末段)                                                                                                |
| 寫信給 Dansk Fjernvarme 要木片到廠價      | ⏸️ **使用者:「之後做」** —— 排入待辦,不是放棄(`DATA.md` §8.5)                                                                       |

### ❌ 更早之前就關閉的

- **爬坡約束** —— 每小時 2.4–9.0 倍滿載,逐時 LP 加了是白做的
- **查天然氣國內碳稅 θ_f** —— 掃描證明它不修時點,還讓尖峰鍋爐離實測更遠
- **挖 2021–2024 的垃圾熱稅真值** —— θ_h=34 壓力測試後連「代理低估」的疑慮都關掉了
- **用行為反推稅費** —— 估計值在不同樣本間 +€25.5 ↔ −€16.5,不可識別
- **確認「穿越點是修 bug 前後跑的」** —— 已查證不受影響(`STATUS.md` §9.6)

## ⑧ 已同意但**還沒動手**的 `plant.py` 重構方向

```
Fuel                  燃料價 + ef + 熱值
Unit(ABC)             共同:P_max, min_load, vom, availability
 ├ BackPressure       η_el, η_th          ← 行為已實作(back_pressure 旗標),但沒拆類別
 └ Extraction         η_el, Cb, Cv, P_max
P2H                   Q_t = k_t·P_buy_t(**一個類別吃 k 向量就好**)
Store                 141b Large TTES
每個 Param 帶 source=("TC", ws, Technology, year, est) 五元組
```

- **`source` 五元組是最高價值的部分** — 把 `STATUS.md` §4 那張手維護的來源表變成機器可查。
- **刻意不開 `P2H(ABC)` + 兩子類**:LP 裡電鍋爐與熱泵是同一條式子,只差 k 是常數還是 COP(T)。
- `min_load` / `forced_outage` / `availability` 放進去當**資料**可以,但要標明 **LP 沒有用它們**。

## ⑨ 絕對不要做的事

- **不要擅自刪 `new_data/` 裡的任何東西。** 它是 gitignored,刪了無法從 repo 還原。
  要改既有檔就用 `varmelast_heat.fill_gaps()` 那種模式:只新增、寫入前驗證舊資料逐格不變、
  先寫 `.tmp` 再 rename。
- **不要把 `heat_cost_per_mwh` 的絕對水準當結論引用。** 仍不可引用的原因:
  ①**2021–2024 生質沒有燃料價** ②抽汽機組容量與熱網規模沒對齊。
  ✅ ③「三個佔位符仍為 0」這一條 **2026-08-15 已解除**(τ/κ 真值、θ_h 區間、θ_f 排除)。
  **方向可引用,水準不可引用。**(唯一例外是垃圾廠的負成本 —— 那個有手算對照且機制清楚。)
- **不要引用舊的 ARC/ARGO 供熱成本數字。** 這個數字被改過三次:
  最舊記錄 −49.95/−51.01(**腳本沒進 repo,重現不出來**)→ 重建後 −47.5(θ_h=0)
  → −23.70(θ_h=23.85)→ **現在 −20.71**(θ_h=26.84)。引用前先確認是哪一組、θ_h 是多少。
- 🔴 **不要引用「θ_h 門檻 = 73 DKK/GJ」與「餘裕 18%」。** 那是舊格點(30 與 35 之間空著)
  造成的假象,2026-08-15 補跑 34 之後同一支程式報 70。**改引用「穿越點 62–70 DKK/GJ」**,
  而且 must-run 的判準要用**供熱佔比**(97.6–99.8%)不是非全開小時。見 ⑤b。
- **不要在儲存層做清理或單位換算。** 見下方工作慣例。
- **不要用 DEA 原型容量或全國平均去填 `dk2_fleet` 的 NOT_FOUND。**
  容量決定 agent 在市場上有多大,而市場力是本論文主題。

---

## 工作慣例(這個專案的規矩)

- **所有指令從專案根目錄跑** — 路徑都是相對的。在子目錄跑會找不到 `new_data/`。
- **`new_data/` 是 gitignored** — 手機/雲端 Claude Code 看不到 ≠ 資料不存在。
- **原始資料存 raw**:不挑欄位、不換單位、不換幣別。
  清理與換算(USD→EUR、公噸→MWh、15分→逐時、丟 0 值、排除錯誤區間)一律在**分析時**做
  → 規則改了不用重抓。
- **抓取腳本要有 skip-if-exists**,絕不覆蓋已抓好的原始檔。
- **每個模組留一個可跑的 self-check**(`python <模組>` 就跑),不用測試框架。
  **self-check 要「重新推導」而不是「比對抄來的數字」** —— 例如 `dea.demo()` 是拿 16 張表
  重算 Cb 的基準,不是硬編一個 0.43。
- **標記不確定性**:佔位值要在程式碼註解裡寫明「這是我設的,未查證」。
  `STATUS.md` §4 是誠實的參數來源清單,四類 (A) 真實資料 / (B) 文獻官方 / (C) 佔位值 / (D) 校準值。
- **commit 訊息用英文**;程式註解與文件用中文。

## 已知的坑(踩過的,別再踩)

| 坑                                 | 說明                                                                                                                                                                                 |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Energinet API 429                  | rate limit 很嚴,連續打會被封幾分鐘。**大量查詢要先抓一次存快取**再本地分析                                                                                                           |
| `ElectricityBalanceNonv` 15 分鐘制 | 2025-10 後轉 15 分鐘一筆。**流量**(MW)加總會把那段算 4 倍權重 → 先 `resample('1h').mean()`                                                                                           |
| **電價也是 15 分鐘制**             | 但價格是**強度量**:聚合一樣用 `mean`,**絕不能 sum**(與上一列是同一件事的兩面)                                                                                                        |
| DEA 目錄 `Cv = 1.0`                | **背壓式的 N/A 哨兵值不是真值**。照抄會讓容量線多一條不存在的限制 → `dea` 已歸零                                                                                                     |
| DEA 沒有 ctrl 中央估計             | **不要自己取 lower/upper 中點**(舊版就是這樣造出 gas_cc 的 300)→ `NoCentralEstimate`                                                                                                 |
| DEA 年份格點                       | 隨技術與 est 而異 → 取最接近年份,不能硬性相等                                                                                                                                        |
| `BE-VL-TOTAL-FAK`                  | varmelast 的 **CO2 排放強度(Kg/GJ)**,**不是熱量欄**。加進 MW_th 分母會算錯佔比                                                                                                       |
| 煤價是 USD                         | `MTF=F` 是 **USD/公噸**;換算在 `load_duckdb.build_fuel()` 做。碳價(ICAP)本來就是 EUR                                                                                                 |
| `el_net` 為負是正常的              | 熱是**義務不是商品**(無熱收入)→ 看 `heat_cost_per_mwh` 才有意義                                                                                                                      |
| 驗證要挑有識別力的標的             | 曾用 CHP 發電量驗熱需求代理 —— 電價影響大得多,**驗不動**。改用 varmelast 真值才驗得出來                                                                                              |
| 比較要控制住其他變數               | 驗「背壓 vs 抽汽」時若同時改 `cv`,就不是純粹放寬,測試會(正確地)失敗                                                                                                                  |
| **年佔比對得上 ≠ 模型對**          | 尖峰鍋爐年佔比 5.95% vs 實測 5.12%,但**日內時點符號相反**(−0.50 vs +0.170)。水準的識別力太弱                                                                                         |
| **季節性陷阱會一再出現**           | 「高價時出力更高」在同一天內幾乎都是反的。任何價格反應的檢定**一律先加日固定效果**                                                                                                   |
| **參數有真值後要追預設值流到哪**   | θ_h 一有預設值,`run_model` 那個「垃圾原型當系統代理」就開始繳垃圾稅 → 尖峰鍋爐從 5.80% 暴衝到 71.65%。看起來像發現,其實是接線錯                                                      |
| **代理模型的錯配不只被抓到那一個** | θ_h 會跳出來只因為它從 0 變非 0。同一個代理裡 `p_fuel`/`ef`/`Cv` **從一開始就錯配**(車隊 64.6% 其實是生質),只是沒有暴露時刻 → **車隊層級數字不可直接比對實測**(`STATUS.md` §9.4c)    |
| **同一個數字在不同文件要對得起來** | θ_h 一天內在 23.8/23.9/23.85/**26.84** 之間動過(先是四捨五入不一致,後是排放係數級距修正)。**引用一律寫 26.84 或 55.6 DKK/GJ_heat**,並註明日期                                        |
| **「兩端一樣」通常是錯的說法**     | θ_h 兩端非全開小時 0.5% → 3.0% 是 **6 倍**,說「無差異」會被咬。要宣稱的是**研究結論**不敏感,不是**數字**一樣                                                                         |
| **稅費要掛對變數**                 | 丹麥垃圾三稅按**熱**計徵,掛在燃料上是錯的。**位置錯了掃描救不回來**(誤差方向取決於內生的 `Q/F`),而且會偽裝成「模型對價格過度反應」                                                   |
| **背壓關係會掩護量綱錯誤**         | `Q = η_th·F` 讓「掛 F」與「掛 Q」在背壓機上恆成比例 → 調參數對得上數字,但抽汽或部分負載就壞                                                                                          |
| **不要用行為反推稅費**             | 電鍋爐開關反推的 τ+κ 在不同樣本間 **+€25.5 ↔ −€16.5**。剛好貼近 soeB25 的 €25.3 是巧合 —— 而且 189 DKK 是**大用戶級距的上界**,DH 廠接高壓層、實付更低,**反推值收斂到 25.3 才該起疑** |
| **水準的敏感度不能推論到時點**     | 「佔比對燃料價 654× 敏感」不代表 ρ 也敏感。燃料價決定 merit order = 決定誰在邊際上 = 正是 ρ 在量的。要另外掃                                                                         |
| **兩端夾要先看有沒有輸出**         | 用「垃圾費 vs 氣價」當兩端,低端尖峰鍋爐全年只有 6 天有出力 → ρ 是 6 天算的,不是估計值                                                                                                |
| **分母要與對照組同源**             | LP 佔比分母是熱需求(消費),實測若用生產分項加總會多 2.44%(蓄熱+網損);且 `Qpb` 對應**氣+油**不是氣單獨                                                                                 |

---

## 先讀哪一份

| 文件                                                               | 內容                                                                                        |
| ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- |
| **[`THESIS_DIRECTION.md`](THESIS_DIRECTION.md)** ← 🧭 **先讀這個** | 新的論文方向:研究問題、為什麼躲得開 λ、量化的三面向後果、章節骨架、誠實的限制、收回過的說法 |
| **[`STATUS.md`](STATUS.md)**                                       | 現況盤點:模型實際長什麼樣、每個參數哪來、哪些是佔位值、哪些還沒做                           |
| [`DATA.md`](DATA.md)                                               | 資料手冊:每個源哪來、留哪些欄、會不會 leak、踩過的坑                                        |
| [`BATTERY_TRACK.md`](BATTERY_TRACK.md)                             | 電池線(已 park)+ 預測管道:設計、結果、結案理由                                              |
| [`LITERATURE.md`](LITERATURE.md)                                   | 文獻庫 ⚠️ **整份還是電池線的**,CHP/區域供熱一篇都沒有                                       |

## 結構

```
new_src/
├── data/       抓資料 → new_data/*.parquet → energy.duckdb
│               calendar_features(spine) / elspot_price(目標 y,逐時+15分) /
│               weather_forecast / energinet_forecast / residual_demand /
│               entsoe_features / fuel_prices(氣/煤/碳/匯率,raw) /
│               biomass_prices(★ 顆粒/木片/秸稈,DST + 瑞典能源署,raw) /
│               production_by_fuel(分燃料出力) / varmelast_heat(DK2 熱需求,含 fill_gaps) /
│               load_duckdb(合併,最後跑)
│
├── heat/       ★ 現行主線:CHP + 區域供熱
│               chp.py          排程 LP(抽汽+背壓 + 蓄熱槽 + 電鍋爐 + 熱泵 + 尖峰鍋爐)
│               assumptions.py  ★ 外部參數單一收斂點(τ/κ/θ_f/θ_h 全有依據,PLACEHOLDERS 已清空)
│               dk2_fleet.py    ★ DK2 六台真實機組與容量(NOT_FOUND 不填補)
│               dea.py          讀 Technology Catalogue → 真實機組參數(含 lower/upper)
│               demand.py       熱需求度日代理(DK1 用;DK2 有真值不需要)
│               calibrate.py    用 DK2 實際熱需求校準代理(R²=0.895)
│               validate.py     ★ 用 varmelast 分項產熱驗排程**時點**(三個尺度無關診斷)
│               fuelmix.py      DK1 燃料組成
│               flexibility.py  彈性價值拆解(逐一關掉選項看成本上升多少)
│
├── battery/    電池線(已 park,見 BATTERY_TRACK.md)
└── models/     統計電價預測(獨立資產,兩條線都能用)
```

## 怎麼跑

```bash
# 熱側(現行主線)—— 每支都有 self-check
python new_src/heat/assumptions.py        # 外部假設 + 佔位符警告
python new_src/heat/dea.py                # Technology Catalogue 參數 + 基準驗證
python new_src/heat/dk2_fleet.py          # DK2 六台機組 + 可跑幾台
python new_src/heat/chp.py                # 排程 LP(抽汽 + 背壓)+ 真實電價示範
python new_src/heat/validate.py           # ★ 用 varmelast 分項產熱驗排程「時點」(約 2 分鐘)
python new_src/heat/calibrate.py          # 用 varmelast 真值校準熱需求代理
python new_src/heat/flexibility.py 2024   # 彈性價值拆解
python new_src/heat/demand.py             # 熱需求代理(DK1)
python new_src/heat/fuelmix.py            # DK1 燃料組成

# 資料(都有 skip-if-exists)
python new_src/data/elspot_price.py       # 電價:逐時 + 15 分鐘
python new_src/data/varmelast_heat.py     # DK2 熱需求(檔案已存在時只補缺季)
python new_src/data/fuel_prices.py        # 氣/煤/匯率(yfinance)+ 碳價(讀 carbon_price_ICAP/)
python new_src/data/biomass_prices.py     # ★ 生質價:DST KN8Y + 瑞典能源署(raw,含三方交叉驗證)
python new_src/data/production_by_fuel.py # 分燃料逐時出力
python new_src/data/load_duckdb.py        # 合併成 energy.duckdb(最後跑,會覆寫)

# 電池線(已 park)— 見 BATTERY_TRACK.md §7
```

⚠️ `load_duckdb.py` **尚未納入 15 分鐘電價** —— `training` view 的右界仍停在 2025-09-30。
要用 2025-10 之後的資料,目前得自己讀 `price15_*.parquet` 並 `resample('1h').mean()`。
